# Changelog

All notable changes to the sk-engines firmware are recorded here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). This project does not yet expose a version constant in the firmware; entries are grouped by the Git tag / release they ship in.

## [Unreleased]

### Added

- **`host/test_gigaverb.cpp` — a host test for `gigaverb`**, the last engine with no off-target exercise and the only one authored through the gen~ path, so this is also the only check that path produces working DSP. It covers the `ParamId` -> gen-parameter map over each slot's own declared range, `GenEngine`'s derived `live_params()` mask, the deck-B drop, the roomsize slew (a raw write retunes the tank's delay lengths and crackles, so `set_param` only stores a target and `tick()` glides toward it), and the reverb tail: a burst then silence rings for ~4 s and decays. The tail is asserted on windowed peaks rather than adjacent blocks, because the tank's energy builds for ~0.5 s as the diffusion wash fills before it falls.

  The test needs `-DGENLIB_USE_FLOAT32` and would otherwise smash the stack on the first block. genlib self-defines it only under `__arm__`, so off-target `t_sample` is `double` while `_ext_daisy.cpp` still casts the caller's `float**` straight to `t_sample**` — `perform()` then reads two floats per sample and walks off the end. The firmware is unaffected, but this is a trap for any future desktop build of a gen~ engine, so `src/engine/gen/genlib_arena.cpp` now `static_assert`s on `sizeof(t_sample)`. That file rather than the bridge, because `_ext_daisy.cpp` is copied from the gen~ export by `scripts/gen_engine.py` and a guard there would be overwritten on the next sync; `genlib_arena.cpp` is project-owned, links into every gen~ engine, and is on the genlib side of the header quarantine so it can see the type at all. [`docs/engine-types/gen.md`](docs/engine-types/gen.md) records it where it describes the boundary.

- **OSC codec for the terminal channel** (`make ENGINE=<e> TERMINAL=1 OSC=1`, [`docs/dev/terminal-osc.md`](docs/dev/terminal-osc.md)). An optional firmware variant that swaps layer [2] of the terminal channel from line-ASCII to OSC over SLIP, so the device can be a node in a Max/Pd/TouchOSC rig where a fader binds to an address once and then just sends floats. Line-ASCII remains the default and the floor.

  It replaces **only** the codec. Layer [1] (transport, SPSC ring, TX FIFO) and layer [3] (verb table, `IEngine` binding, `mode test`, `describe`) are shared byte for byte with the line build: an address resolves to a line in the existing grammar and goes through the existing `dispatch_line()`, so there is no second verb table and no second error taxonomy. `/sk/a/param/speed ,f 0.5` becomes `set param speed a 0.5000`.

  The address is the **layer-2 slot**, never the engine's meaning — `/sk/a/param/speed` is the PITCH knob on every build, and `radio`'s "station" travels as a cosmetic label in `describe`. That split is what lets one control-surface layout drive every engine, and it is why the engine-specific namespace (`/radio/a/station`) is generated **host-side** by `tools/skdev/semantic.py` rather than living in the wire format where a hand-maintained table could rot into a protocol bug.

  Reads are **arity, not a verb**: a message with no arguments reads the address it names, which is available only because the value lives in the argument rather than the path. Writes are silent by default (a rig streaming fader moves at 100 Hz does not want an ack per message); `/sk/dev/mode/ack ,T` turns acks on per session.

  Four things the implementation had to correct in the spec, all recorded there: the footprint estimate was low by roughly 4x (**measured ~9.0 KB `SRAM_EXEC` + ~12.4 KB SRAM** over the line build, the largest single item being a 6 KB scratch the estimate omitted entirely — a bundle cannot be assembled incrementally into a ring buffer, because each element carries its own length prefix); the descriptor bundle is 5548 B unmasked rather than 2–3 KB, taking the TX FIFO to 8 KB; typing the `query` reply needed two more `TextSink` methods than "make it virtual", because that one call site types its value in a table the sink cannot see; and the spec's own `describe` state row was wrong — `,sss` dropped the Enum selector labels that the line codec *does* send, so the two codecs described the same device differently. Now `,ssss`.

  **`pstretch` cannot host this build.** `TERMINAL=1 OSC=1` fails to link there, `.bss` overflowing `SRAM` by 4352 bytes — its line build already sits at 97.4% of SRAM because the FFT working set needs its own linker split. Not close, and not a regression; it is the case the spec always predicted. Every other engine that hosts `TERMINAL=1` hosts OSC (`delay` 72.3% of `SRAM_EXEC`, `tape` 79.5%).

  `OSC=1` currently forces `DEBUG=1` off as a build error: a `[tag]` log line interleaving on the shared CDC is harmless for line-ASCII and *fatal* for SLIP, where it lands inside a packet. Wrapping log output as `/sk/log` frames is the right fix and is still open.

  **Sixteen engines carry `param_label()` tables** (115 labels), so the label half of the design is real across the catalogue rather than latent: `reso` advertises Rings' own patch fields (`size`→*damping*, `modamp`→*structure*), `mosc` advertises Plaits' front-panel names (`size`→*harmonics*, `pos`→*timbre*), `edrums` renames almost everything because it is a sequencer (`pos`→*density*, `size`→*pattern length*), and `bard` renames the panel entirely (`speed`→*book*, `pos`→*bookmark*). Three abstentions are deliberate: **granular** gets none because the shared `ParamId` vocabulary *is* granular's own words, so a table would be a second copy of `kParamNames` that could drift from it; **csound/chuck** label only the patch selector, because every other slot is a pass-through whose meaning the loaded patch defines and a fixed label would be a lie; and **gigaverb** is skipped because `GenEngine` has no slider names to derive from and its header is generated, so the fix belongs in `scripts/gen_engine.py`. `host/test_osc_labels.cpp` links 11 engines and enforces, for each, that every advertised address is composed as the rules predict, that both decks agree, that no two live slots share a label, and that nobody renames the platform crossfader.

  **`chorus`, `filter` and `voice` get theirs by derivation instead**, from the Faust bind table their manifest already generates — the slider label *is* the layer-3 name (`filter` binds "cutoff" to `ParamId::Speed`, "drive" to `Size`). Implemented once on `FaustEngine`/`FaustChainEngine`, so every present and future generated Faust engine is covered with no per-engine code and nothing that can drift from the manifest. This mirrors what those wrappers already did for `live_params()`, which they derive from the same table. Where a chain engine binds one role in both stages — `voice` puts `Speed` on the oscillator's "freq" *and* the filter's "cutoff" — stage A wins.

  Verified off-target by `host/test_terminal_osc.cpp` (SLIP, wire format, coercion, every address family against the exact `IEngine` call, reply typing, ack mode, errors, inbound bundles, `describe`), `host/test_osc_labels.cpp` and `tools/test_osc_codec.py` — and **on hardware**, by the cross-codec parity sweep the spec makes its acceptance criterion: a cased Spotykach running `tape`, **63/63 identical against both codecs** (`make test-hw` vs `make test-hw CODEC=osc`). Because the sweep's cases are generated from `describe`, an identical result list also proves both codecs advertise the same param, config and query sets. The ~4 KB descriptor bundle arrives whole over the 64-byte USB packet path, and a steady-state round trip measures 0.18 ms.

- **The web terminal speaks the OSC codec.** The browser front-end could talk only to a line-codec build; a `TERMINAL=1 OSC=1` device was unreachable from it. Four layers landed, each a port of its `tools/skdev/` counterpart so the two front-ends stay diffable against one reference: `web/src/core/osc.ts` (SLIP + the OSC 1.0 wire format), `web/src/core/oscdevice.ts` (the client, reducing the describe bundle to the same `Descriptor` the line codec's `parseDescribe` produces), `OscSerialTransport` behind a new `FrameTransport` port, and `web/src/core/client.ts`, one `DeviceClient` surface over both.

  That last one was the actual blocker rather than the codec. The generated control surface composed command strings inline — `set param ${name} ${deck} ${v}` — which is a line-codec *sentence*: against OSC it is not a different spelling of the same request, it is not a request at all. So a surface built from strings stops working the moment the codec changes, and stops by sending nothing, which looks like a dead device. It now drives named operations that each client implements.

  The codec is chosen before connecting and locked for the session, because it is a property of the firmware, not of the connection; picking wrong yields a device that never answers, so the choice is visible rather than inferred. The console takes OSC addresses on an OSC build (`/sk/a/param/speed 0.5`, and the bare address as a read) and refuses line commands with a message saying so — deliberately not translating them, since that would be a second hand-written copy of the composition rules `scripts/sk_osc.py` derives from the firmware tables, free to drift into `unknown-address` rather than into an obvious failure. Console arguments are typed from how they were **spelled**, so `2` is an int and `2.0` a float; that ambiguity has a good answer only here, which is why every other path defaults a plain number to `,f`.

  284 tests, up from 206. The codec suites decode `host/build/describe_osc_sample.bin` — bytes from the real firmware encode path via `host/test_terminal_osc.cpp`, not a hand-written fixture — and skip cleanly when the host suites have not been built, which is what the test harness gained a `skip()` for. **Host-verified only: none of this has met a browser or a device.** [`web-frontend-checks.md`](docs/dev/web-frontend-checks.md) gained C8b for that pass.

- **`tools/skbridge.py` — a UDP↔SLIP-serial bridge, so OSC-over-UDP software can reach the device at all.** This closes a hole the OSC work had left open and which nothing in the repo acknowledged: TouchOSC, Max, Pd and every `oscsend`-shaped tool emit OSC over **UDP**, and the device has no network interface — a Daisy has neither Ethernet nor WiFi, so it is OSC over SLIP over USB CDC. `make tosc` was therefore generating TouchOSC layouts with nothing to plug them into, and the OSC codec's only reachable consumer was the browser front-end (which drives the serial port directly). Same arrangement, and the same reason, as monome's `serialosc`.

  The bridge **does not parse OSC**. UDP is already packet-delimited by the datagram and SLIP supplies the same delimitation over a byte stream, so a packet crosses unmodified in both directions and a new address, argument type or bundle layout needs no change here. Replies go to the sender's IP on a fixed receive port, because an OSC client sends from an ephemeral port and listens on a configured one. It also names the failure that is otherwise pure silence: bytes arriving with no SLIP framing among them is what a *line*-codec build looks like from the bridge, and it says so.

- **`tools/test_liblo_conformance.py` — conformance against an OSC implementation nobody here wrote.** Everything else validating this codec is in-house (the C++ firmware encoder, the Python client, the TypeScript client), and three implementations by one author agreeing proves they share an understanding, not that the understanding matches OSC. liblo — the reference C implementation, what `oscsend`/`oscdump` are built on — is loaded through ctypes and used both ways round. Results: liblo's serialiser is **byte-identical** to ours across float/int/string/true/false/multi-argument messages, liblo **accepts every one of the 24 rows** of the real firmware describe bundle, and liblo's own `lo_send_message` reaches the far end of a pty **through the bridge**, byte-identical to what the device's encoder produces. So the wire format is genuinely OSC, not OSC-shaped.

  It also found the one place the two disagree, which was worth having in a test rather than in someone's memory: **liblo cannot omit the type-tag string.** This codec spells a read as a message with *no* type-tag string at all — legal OSC 1.0, and the basis of "arity, not a verb" — where liblo, python-osc and Max all emit at least a bare `,`. The packets differ on the wire. Both must mean zero arguments or every read from every third-party client fails while the in-house ones pass, and it would fail as silence rather than as a rejection. They do converge, because the firmware's tag loop simply never executes on an empty tag string; `host/test_terminal_osc.cpp` now asserts that against its own decoder instead of leaving it accidental.

- **`/sk/log` framing — `OSC=1 DEBUG=1` now builds and works.** It used to be a hard Makefile error, the phase-1 shortcut the OSC spec called "acceptable": the Logger and the terminal share one CDC device and the Logger writes raw ASCII, which is harmless interleaved with line-codec replies but lands *inside* a SLIP frame and corrupts it. A corrupted frame is neither valid OSC nor recoverable, so the symptom is a device that appears to stop answering rather than one that logged something.

  Option (b) from the spec is now implemented ([`src/terminal/osc_log.cpp`](src/terminal/osc_log.cpp)): each log line goes out as `/sk/log ,s` in its own frame. The guarantee has two halves — the Logger is pointed at `LOGGER_NONE` on an OSC build so a stray `Log::Print*` is discarded rather than corrupting the stream, and `LOG_TAGGED` is rerouted to the framed path. It is gated on `INFS_LOG` exactly as the Logger path is, so both codecs agree about when logging exists and a release image does not start emitting frames the line build suppressed.

  Logs are best-effort and never displace a reply: a line is written only if its encoded size plus a 1 KB reserve fits, and is dropped whole rather than half-written. One message is held from before the terminal exists, because `LOG_TAGGED("boot", ...)` necessarily runs before `_terminal.init()` and the banner is the line most worth seeing. Both OSC clients skip `/sk/log` as they skip `[tag]` on the line codec — `web/src/core/oscdevice.ts` already did; `tools/skdev/oscdevice.py` gained it here, with an optional `log_sink`. Cost: **nothing in a release image** — the bind call is gated on `INFS_LOG` too, so `--gc-sections` drops the whole path and the ELF carries zero `osc_log` symbols. With `DEBUG=1` it is ~1.7 KB of `SRAM_EXEC` (delay, `TERMINAL=1 OSC=1`: 72.36% → 72.99%).

- **`make test-tools`** — the device-free half of `tools/`: the `describe` parser, the OSC codec + semantic translator, the UDP↔SLIP bridge, and conformance against liblo. The first two are checked against byte samples the off-target C++ tests emit rather than hand-written fixtures. Needs no hardware, so unlike `make test-hw` it runs in CI, where it was previously uncovered (`make test-scripts` only ever ran `scripts/`). `tools/conftest.py` defers its pyserial import into the `device` fixture so those tests collect on a machine that has only pytest; the bridge's pty rig and the liblo checks skip cleanly rather than failing when pyserial or liblo is absent, which is why CI installs both — a silent skip would quietly delete the conformance evidence instead of reporting it.

- **`make tosc` — generated TouchOSC control surfaces** ([`docs/dev/tosc.md`](docs/dev/tosc.md)). One `.tosc` layout per engine plus a universal one, written into `dist/tosc/` by [`scripts/gen_tosc.py`](scripts/gen_tosc.py) from the address model in [`scripts/sk_osc.py`](scripts/sk_osc.py), using [py2tosc](https://github.com/shakfu/py2tosc) (added to the `dev` dependency group). This is the consumer [`docs/dev/terminal-osc.md`](docs/dev/terminal-osc.md) names at the end of its "Where it lives" section, and it is where that document's two-tier split pays off: a fader prints `station` on a radio build while binding to `/sk/a/param/speed`, so the surface never translates at run time. **A layout reaches the device through `tools/skbridge.py`** — TouchOSC speaks UDP and the hardware has no network interface, so until that bridge landed these layouts had nothing to plug into; `docs/dev/tosc.md` now carries the connection procedure.

  **The address space is parsed out of the firmware, not restated.** `sk_osc.py` reads `kParamNames`/`kConfigNames`/`kConfigLabels` and the two scope predicates from `src/terminal/names.cpp`, the `kPlatformQueries` table from `src/terminal/dispatch.cpp`, the `ParamId`/`ConfigId` enums from `src/engine/engine_params.h`, each engine's `live_params()`/`live_configs()` from its header, and the `ENGINE=` branches from the Makefile. Only the *shape* rules are written in Python — which kind segment a leaf takes, when a deck segment appears, the closed sets of stimulus and platform verbs — because those live in the design document and nowhere in the source. The alternative, transcribing the document's address table, is a second copy of the vocabulary free to drift the moment a `ParamId` is added; here, adding one is followed by `make tosc` and nothing else.

  The composed space comes to **135 addresses**, matching `terminal-osc.md`'s own Totals table exactly — 17 deck params, 4 global, 5+1 configs, 6+2 state, 19+3 stimulus, 14 platform. The failure mode being defended against is not a stale copy but a regex that quietly stops matching: it would return an empty table and compose a smaller surface with no other symptom.

  **The shape rules are asserted against `osc_addr.cpp`, not against the document.** Which kind segment a leaf takes, when a deck segment appears, the closed stimulus and platform verb sets — those exist in the design document and in the resolver, and in no table either side can share, so nothing made the two agree. `scripts/sk_osc.py --inventory` exports the whole space to `host/osc_addresses.txt` (committed, with the same staleness guard the `web/` export carries) and `host/test_osc_addr.cpp` puts every line through the real `osc_dispatch_packet`, in both directions: every address the model composes must resolve, and every address `describe` advertises must be in the model. The first direction also probes deliberate near-misses — `/sk/a/size` with no kind segment, a global param carrying a deck, a platform-owned id — so a resolver that accepted everything could not pass it. 135 probed, 0 unresolved; 62 advertised, 0 missing.

  It found a firmware defect on the first run — see **`describe` advertised the wrong address for the four platform reads**, below.

  Per-engine layouts are the same surface narrowed by the engine's liveness masks — fewer controls, none of them dead, since an address for a masked-out slot is `unknown-address` on the device rather than a silent no-op. Four engines (`chorus`, `filter`, `voice`, `gigaverb`) compute their masks in a loop over a binding table, which cannot be read out of a header; they get the universal surface and the generator says so. `--describe` takes a captured `describe` block instead and narrows them, and a test asserts that the two routes compose an identical surface for `radio`.

  Three bindings are semantic rather than cosmetic. **Reads send nothing** — an argument is what makes a message a write, so every read button and bare trigger carries an empty argument list and fires on RISE only. **Params listen on the reply mirror** (`/sk/reply/<same path>`, receive-only and trigger-less), except the `ab` fan-out knobs, which cannot read because one request has no way to have two answers on one reply address. **Every bound control carries its address in `tag`**, so the layout is readable without unpicking message partials.

  Layer-3 labels are read from `param_label()` in the engine headers, alongside the liveness masks — including the three generated Faust engines, whose bind table the parser reads directly even though the mask that walks it is not statically readable. This started as a curated `scripts/tosc_labels.json`, written while `param_label()` was still a proposal; that file is **deleted**, and what happened to it is worth recording, because it is the hazard the parse-not-restate rule exists to prevent. Within one merge it had already drifted — it called shuttle's `Speed` "capstan" against the firmware's "varispeed", and its `Aux` "slot" against "tape slot". An unlabelled slot prints its layer-2 name, which is the firmware's own fallback; `granular` declares none deliberately, since the shared `ParamId` vocabulary is already its words.

  **Status: opened in the TouchOSC editor and renders correctly; not yet pointed at a device.** The first editor run did find a real defect — pages had been sized with proportional weights, which decide cell *shape* only by accident, so the knobs came out in 168×98 cells and TouchOSC drew each RADIAL as a clipped ellipse. Pages are now laid out in pixels: each band declares a wanted cell height as a fraction of its cell width, the page fits them to the canvas by scaling every cell together, and knobs are inset to a square inside whatever cell they get. 39 tests cover the model, the bindings and the geometry, and none of them needs a device.

- **Continuous integration** ([`.github/workflows/ci.yml`](.github/workflows/ci.yml)). A run builds **23 firmware images** through the canonical Makefile path — all 20 engines that need no external runtime, plus `TERMINAL=1` on `delay` and `pstretch` and `TERMINAL=1 OSC=1` on `delay` — runs the four off-target suites and `check-boundary`, and builds six engines through the **CMake** path as well.

  The engine matrix is the point. Two engines had stopped building outright and stayed that way undetected (`chorus` at 145% of the data region, `pstretch` overflowing `SRAM` by 80 KB), both because a change to the memory-region split was never re-checked against them; neither is the kind of failure a test suite catches, because there is nothing to run. The CMake subset is chosen by flags rather than coverage — `pstretch` (own linker script), `reverb`/`graincloud` (`-Os`), `softcut` (extra sources), `mosc` (QSPI boot), `granular` (default) — and would have caught every one of the seven divergences recorded below.

  `csound` and `chuck` are deliberately kept out of that matrix: each must first fetch and cross-build a large gitignored runtime, tens of minutes apiece. They live in [`qspi-libs.yml`](.github/workflows/qspi-libs.yml), with the cross-built archive cached against the fetch script that pins it.

  The `tests` job installs **pytest, pyserial and liblo7**. The last two are not incidental: without pyserial the bridge's pty rig skips and its I/O half goes untested, and without liblo the only conformance check by an OSC implementation nobody here wrote skips too. Both skip *cleanly*, so an uninstalled dependency reads as a pass rather than as missing coverage.

  **Triggers armed 2026-08-11.** `ci.yml` runs on every push and pull request; `qspi-libs.yml` stays off push/PR (a run cross-builds two large runtimes) and instead runs weekly, Mondays 04:00 UTC, plus on demand before a release. The workflows shipped `workflow_dispatch`-only for an initial period — the failures CI exists to catch arrive with a commit, not with someone remembering to press a button, so that state was always temporary. Note that neither workflow has yet completed a run on a real GitHub runner, so the first push is also the first proof; the likeliest friction is the `carlosperate/arm-none-eabi-gcc-action` pin (`10-2020-q4`, matching the local 10.2.1).

- **`make test`** — one target for all four off-target suites plus the boundary guard, and **`make help`**, an index of the ~50 targets and the dozen build toggles. `test` had to be declared `.PHONY`: without it make matched the `test/` *directory*, called it up to date, and did nothing — a footgun the README had been documenting rather than removing.

- **Host tests for `mosc` and for granular's audio path**, closing the two coverage holes the review named.

  `host/test_mosc.cpp` instantiates and renders **all 24 Plaits engines** — each one a different DSP object with its own state, resources and (for several) its own arena allocation — asserting each is finite, bounded and, for the pitched models, audible. It also pins Gate vs Drone, the output level, and the `live_params()` round-trip the on-device `describe` sweep trusts. mosc was the largest engine in the tree with no off-target coverage at all, and being a QSPI build it is not even exercised by the normal `ENGINE=` sweep, yet it ships as a published binary.

  `host/test_granular_audio.cpp` covers what `test_engine_params.cpp` does not: **audio**. That test drives granular's parameter surface but never puts a sample through the engine, so a break anywhere in buffer → deck → generator → output would have left the suite green. The new one loads a known level through the storage port and asserts the output level scales with it (measured 3.77× for a 4× input step), records live input and plays it back, and renders all three modes.

  Slice needed a gate to make a sound, which is correct rather than a bug: `Deck::play()` deliberately dispatches nothing for Slice ("going to be triggered from tick method"), so with an empty sequencer track it is silent until something fires a slice.

- **`make dist-cmake` — a full release build through the CMake path.** Runs the same `scripts/build_release.py` as `make dist` with `--cmake`, so the manifest, `SHA256SUMS`, release notes and the in-binary banner check are shared code and only the compiler driver differs. It exists to compare the two build systems across the whole engine set; the first run produced all 18 published engines, and the resulting size table is now the evidence in [`docs/dev/cmake-gap.md`](docs/dev/cmake-gap.md). Three things it needed:

  - **`SPK_VERSION` was not overridable in CMake** — `execute_process(git describe)` clobbered any passed value. That was a hard blocker, not a nicety: the script *verifies* the banner it expects is present in the `.bin`, so every artifact would have failed that check. Now guarded, matching the Makefile's `?=`. A parity gap in its own right.

  - **No `clean` step, and no `ENGINE_MAKE_FLAGS`.** The canonical path cleans between engines because `build/` is shared and its stamps force only a subset of TUs to rebuild; CMake's per-engine directory has no cross-engine contamination to wipe, and cleaning would rebuild libDaisy once per engine. `APP_TYPE`/`LDSCRIPT` are Makefile concepts — `CMakeLists.txt` derives the linker script from `ENGINE` alone.

  - **Output goes to `dist-cmake/<version>/`, never `dist/`.** `make gh-release` globs `dist/<version>/*` and uploads it, so a shared directory would let someone publish CMake-built binaries as an official release by running two targets in the wrong order. These binaries are **not for shipping**: ~1.25 KB larger and not byte-identical. `dist-cmake/` needed its own `.gitignore` entry — `dist/` matches that name exactly and `build-*/` does not apply.

  Deliberately **one** entry point: adding a `dist` target to `Makefile.cmake` as well would give two ways to invoke the same script, which is the duplication that produced every drift recorded above.

- **A browser front-end for the SD card tooling, plus a WebSerial terminal** (`web/`; TODO P6, design in [`docs/dev/web-frontend.md`](docs/dev/web-frontend.md)). The CLI that landed for the card solved the *knowledge* problem but left an *access* one: using it needs a checkout, Python, and a decoder — ffmpeg, or cysox plus a libsox built with the right format handlers. For someone who bought a device and wants audio on a card, that is still a developer's toolchain. A browser needs none of it, and one API changes the calculus outright: `decodeAudioData` decodes mp3/flac/wav/ogg identically on every machine, so the *entire* decoder-backend apparatus in `sk_card.py` — the cysox probe, the ffmpeg fallback, the per-file `find_format` check — simply does not exist in the web version. That half is **simpler** than the CLI, not a reimplementation of it with extra steps. A static page: no server, no dependencies, no build step (`make web-serve`, `make test-web`, `make web-data`).

  - Tabs run **Build, Convert, Verify, Reference, Terminal** — the order a person needs them, not the order of their value. **Build** is where a fresh visit lands: one click produces a complete, valid, minimal card (folders, configs, a README in each folder, the example patches), byte-identical to `sk_card.py init --no-demo`. **Convert** decodes and re-encodes to exactly what the target engine reads. **Verify** is the most valuable screen but the wrong *first* one — pointed at a blank card it can only say "this is not a card" — so it sits third, where a user reaches it with something to check; it needs the least from the browser, reading only filenames and headers. **Reference** is `sk_card.py layout` as a screen — what every engine expects on the card, the constraints that fail silently, the sidecar defaults (`radio/rate.txt` decides the playback rate of a whole bank and was documented nowhere in the UI), and the `SK/config.txt` properties with their ranges; it sits after the task tabs because it is the lookup consulted *while* doing the job rather than a step in it, and it is the only tab that asks nothing of the browser, so it behaves identically everywhere. **Terminal** talks to a `TERMINAL=1` build over USB CDC and *generates* its control surface from the device's own `describe` reply — a slider per advertised param with its real range, and nothing for the enum entries this engine ignores.

  - **Nothing about the card layout is declared in JavaScript.** `card_layout.py` gains a `--json` export that carries the table, the scan rules, the format specs **and every piece of generated text** (the per-folder READMEs, the root README, the default config). That text is a pure function of the table and is the part a JS port would most plausibly reimplement and then let rot, so shipping it as data means the browser builds a card **byte-identical** to `sk_card.py init` — asserted per file by SHA-256 — while owning none of the wording. Convert's target naming became a per-bank template (`Bank.target`) consumed by both front-ends, replacing six per-engine branches in `sk_card.py`; the checkers' shared rule sets (`SOURCE_EXTENSIONS`, `CONFIG_PROPERTIES`, `SIDECAR_NAMES`, `SKIP_DIRS`) moved to `card_layout.py` for the same reason. No behaviour change to the CLI.

  - **The two things that genuinely are reimplemented are pinned in both directions.** The WAV/raw writer is asserted byte-identical against fixtures generated by `card_audio.py` (the test that bites: Python's `int(s * 32767.0)` truncates toward zero, so a JS port using `Math.round` differs on about half of all samples and nobody would hear it until a card sounded slightly wrong). The `verify` walk is asserted to reach the same verdicts **and the same fix text** as the real `verify_card`, on a deliberately-broken card that `web_export.py` builds and runs through the Python — so the expectation is the Python's actual behaviour, not a second reading of the spec. `make test-scripts` fails if the committed export has drifted from `scripts/`; `make test-web` fails if the JS disagrees. 135 JS tests, 34 Python tests across `scripts/test_web_export.py` and `scripts/test_md2html.py`.

  - **Chromium-only APIs are designed around rather than required.** WebSerial and File System Access are absent from Firefox and Safari, which for a music-hardware audience skewing Mac is a real cost. Verify/Build/Convert fall back to drag-in-files → download-a-zip (via a dependency-free ZIP writer with reproducible timestamps, matching `sk_card.py dist`); only in-place card editing and the terminal need Chromium, and each says so where it is missing. Out of scope, deliberately: firmware compilation, DFU flashing (the [Daisy Web Programmer](https://electro-smith.github.io/Programmer/) already does it and a half-written image is the worst failure in the system), formatting the card, and demo-audio synthesis — the released `sk-card-<version>.zip` is already checksummed, so the page links it rather than producing a second unverifiable copy.

  - **Rewritten in TypeScript, and re-layered so the model is separable from the view** (`web/src/`, bundled by [bun](https://bun.sh) to a committed `web/dist/app.js`). The original design made "no build step" a constraint and this overturns it — knowingly, and more narrowly than it sounds: the bundle is committed, so *serving* and *deploying* still need no toolchain (GitHub Pages serves `web/` as-is, a fresh checkout opens the page), and only *editing* does. The cost bought two things that were not otherwise reachable.

    - **Types on the parts that cross a boundary.** `card_layout.json`'s shape is declared once in `core/types.ts` and checked everywhere it is read, so the export growing a field or losing one is a compile error rather than an `undefined` on a screen. `tsc --noEmit` runs in `make test-web`, strict for `src/` and with null-checking relaxed for the tests — the split is deliberate: strictness earns its keep where an unhandled `undefined` reaches a user, and in a test it mostly demands `!` after every fixture lookup the next line asserts is present.

    - **Four layers, with dependencies pointing inwards.** `core/` is the rules and touches no browser API at all; `platform/` is the only place the four browser-only APIs live (File System Access, Web Audio, WebSerial, CompressionStream) plus `setInterval`; `app/` is one view-model per tab holding all the state and none of the DOM; `ui/` renders and binds. Everything the browser owns enters through an interface in `core/ports.ts`, which is what makes the view-models testable against twenty-line fakes.

    - **The payoff is `test/model.test.ts`**: questions that used to need the DOM shim, and two that needed *hardware*, are now ordinary unit tests over plain objects — does Convert decode at the rate the bank demands, does a decode failure leave the tab recoverable, does an empty port chooser explain itself, does an unplugged device tear the session down, does the CPU poll give up when the build cannot answer. 206 tests, up from 136.

    - **The layering is enforced, not asserted.** Three tests fail on a browser global in `core/` or `app/`, on an import that points outwards from either into `platform/`, and on a browser-only API used outside `platform/` — because an architecture claim rots silently, and one convenient `document.` in a view-model puts a browser back in the way of every model test. A fourth fails when the committed bundle is older than `src/`, since every test imports the sources directly and would otherwise stay green against a stale page. Writing the ZIP compressor as an injected port (`Deflate`) rather than an import fell out of the same rule, and made both branches — compressed, and stored because compression did not help — reachable without a browser for the first time.

  - **A page per engine, with the documentation rendered in place** (`web/engines/<name>.html`, generated by the new `scripts/md2html.py`). An Engines menu in the System 6 menu bar lists all 22 documented engines; picking one opens its own page - the card format it expects, then its full manual with its control diagram inline. `#engine/<name>` is a real route, so a link to one is shareable, and a link between two engine docs stays inside the app instead of leaving it.

    - **The markdown is converted at export, not in the browser.** Shipping a markdown parser would be a runtime dependency for a page that has none, to parse the same 22 fixed files on every visit. `md2html.py` is a deliberate *subset* converter - the constructs these docs actually use, established by surveying them: headings, paragraphs, fenced code, pipe tables, lists, blockquotes, rules, images, links, bold, italic, inline code. Anything outside the subset stays escaped text, so an unsupported construct looks plain rather than broken.

    - **It is escaped, because the output is injected with `innerHTML`.** Every piece of source text is HTML-escaped before a tag is inserted. Feeding the converter a deliberately hostile document then found a real hole - `[x](javascript:alert(1))` produced a live handler - so hrefs are now scheme-checked after rewriting, since it is the final URL that reaches the page. Nothing in `docs/engines/` contains one, but a generator that *can* emit it will the day somebody pastes a link. Tests assert it on both sides: the converter refuses, and the committed fragments contain no live markup.

    - Three fixes from looking at a real page. The engine's citation line was printed **twice** - once from `engines.json` as raw markdown with its backticks showing, and again inside the rendered fragment where it belongs. The documentation column was capped at the 66-character reading measure, which is right for prose and wrong for reference manuals full of control tables; it is 88 now. And `system.css` sets `.window { overflow: hidden }`, so anything wider than the window was **clipped with no scrollbar and no warning** - a long path in a paragraph was enough to make text silently disappear. Tables and code already scrolled inside their own boxes; everything else now breaks rather than overflowing.

    - Two bugs the work surfaced. Splitting inline text on code spans put the two halves of ``**a `b` c**`` in different fragments, so neither matched and **72 literal `**` shipped across the docs** - fixed by lifting code spans to placeholders and keeping the run contiguous. And the extraction that feeds the Engines menu was too rigid for how much these docs vary: 16 of 22 had no summary, five no description at all, and `bard` exported its status blockquote as its description.

  - **The diagram palette now labels rather than tints, and the group titles are legible for the first time.** For `shape: sql_table` d2's two style keys do not mean what they sound like: `fill` paints the header row and `stroke` paints the table body, not a border. The template had them the wrong way round, so every table came out tinted hard enough to fight its own text while the header stayed near-white. The colour moved to the header and the body went white.

    - The LED circles got a class of their own and stayed uncoloured - white, with a dark outline. They are not tables, so the same two keys mean the ordinary thing there and `class: knob` had been filling them solid. Uncoloured is also the right answer on its own terms: a ring stands for a physical light whose colour is what the caption beside it describes, so tinting the drawing states a colour that contradicts its own label.

    - The real defect was underneath that, and only measuring found it: **d2 renders a table's header label in white**, on the assumption that the header fill is dark. Against the old near-white header that is **1.11:1** - the title was there and invisible, which is exactly what the screenshot showed. `font-color` is now set per class, and dark text on these pastels reads at 11:1 or better. Simply swapping fill and stroke would not have fixed it: white on the mid-blue is still only 1.72:1.

  - **The control diagrams are half the width they were, because half of one was a duplicate.** Every spec fills Deck A and Deck B from the same knobs/pads/ring, and only two of nineteen engines (`gigaverb`, `voice`) actually differ per deck - so seventeen diagrams rendered the identical table twice, side by side. That duplication was most of their width, and their width was why they scaled to a third of readable however large the viewer got. `gen_controls_diagram.py` now collapses the two columns into one labelled "Deck A and B (identical)" when the spec has no `deckB` override, and the template stacks each deck's knobs, pads and ring rather than spreading them across. The asymmetric two keep both columns, where the second is information rather than an echo.

    - The result, measured rather than eyeballed: the widest diagram went from 3.81:1 to 1.58:1 and the median to 1.23:1, and a diagram fitted to a 1900x1200 view now renders at 59% of natural size instead of 33%. That is the difference between labels you can read and labels you can see.

    - **And a third: visible is not the same as useful.** Once they rendered, the control diagrams were about a third of their natural size and their labels were illegible - a picture that is there and worth nothing. Two changes. The reading measure moved off the column and onto the prose, so a paragraph still wraps at 88 characters while figures and the wide control tables get the whole pane. And each diagram is now a link to itself with a caption saying so: at any width this page can offer, a 4000-unit-wide diagram will not be readable inline, so the inline copy is honestly a thumbnail.

    - **The diagrams needed two fixes, not one.** After the paths were corrected they still did not appear, and the second cause was in the files: D2 emits `<svg viewBox="0 0 4045 1335">` with no `width` or `height`. Inside an `<img>` that is an intrinsic RATIO with no intrinsic SIZE, so `height: auto` had nothing to resolve against. An explicit `width: 100%` makes the ratio do the rest. The test asserts both halves - that the files really are dimensionless, and that the stylesheet compensates - so if a future toolchain emits dimensions the rule can be retired rather than becoming cargo cult.

    - **The diagrams were 404ing, and the test agreed with the bug.** Image paths were written relative to the fragment (`media/x.svg`), but a fragment is fetched and injected into `web/index.html`, and a browser resolves relative URLs in injected markup against the **document's** URL - so every control diagram was requested from `web/media/` and nothing appeared. They are page-relative now (`engines/media/x.svg`). The test that should have caught it resolved the path the same wrong way, joining the fragment's directory, so it passed on a broken page; it now resolves against the document, as the browser does, and was re-verified against the old paths.

    - The exporter now reads each SVG's viewBox and puts the real width and height on the `<img>`. That fixes the missing intrinsic size at the source instead of with a CSS workaround, and it is what makes "actual size" meaningful - natural size has no meaning until the natural size is known. A test cross-checks each tag's numbers against the file's own viewBox, so a wrong number fails rather than any number passing.

    - **A click-to-expand viewer for the diagrams**, replacing the open-in-a-new-tab that preceded it. It is a `<dialog>`, like the About box, so Escape, the backdrop and the focus trap come from the platform. It offers two views because neither answers both questions on its own: **Fit** shows the shape of a diagram, **Actual** shows what its labels say, and a four-thousand-unit-wide drawing cannot do both at once on any screen. It opens on Fit, and switching to Actual centres the pan rather than dumping the reader on the left margin. The generated markup still wraps each diagram in a link to the file, so the viewer is an enhancement over something that works without it rather than a replacement for it.

  - **A print-ready PDF beside every control diagram.** The SVG is for the screen; a control surface is a thing people print and pin next to the hardware, and that wants vector PDF. `make diagrams` now emits one per control diagram, and each engine page offers it - in the caption, so it works with scripting off, and in the diagram viewer's toolbar.

    - Rendered by **librsvg, not d2**. d2 can emit PDF, but only by driving a headless Chromium it downloads on first use, and that download 404s from its CDN. `rsvg-convert` renders the SVG d2 already produced, needs no browser, and keeps the text as text - the output carries 41 embedded font objects rather than a rasterised picture of the labels, which is the whole point for something destined for a printer. A test asserts that, since a silent fallback to raster would print badly and look fine on screen.

    - One trap worth recording: the caption's download link and the diagram's own link both sit inside the same `<figure>`, so the viewer's click handler had to be narrowed to the link that wraps the image. Intercepting both would have turned "download PDF" into "open the picture again".

  - **The front-end now looks like Mac System 6**, via [system.css](https://github.com/sakofchit/system.css) (MIT, vendored in `web/vendor/` rather than fetched from a CDN, since the page is a static deploy). A deliberate choice of character for a tool whose audience builds Eurorack modules — one window on a desktop, a title bar, Chicago, framed buttons. It is 20 KB of CSS plus 32 KB of fonts and button frames, and it replaced the base layer rather than sitting beside it: `app.css` stayed roughly the same size, because what it holds is the components system.css has no opinion about.

    - **It cost the colour, which is the interesting part.** system.css is genuinely 1-bit — the whole palette is black, white and two greys — and severity used to be carried *entirely* by colour: red, amber and green rules down the side of each finding. That encoding is gone, replaced by three that are not colour: the level as a **word** in the markup (ERROR / WARNING / OK, so it is selectable, translatable and spoken, which a CSS `content` string would not be), **inversion** to white-on-black for errors, and **rule weight** — solid, dotted, hairline. The result is strictly more robust than what it replaced, which conveyed nothing to a colour-blind reader that the group heading did not already say. Dark mode is gone with it; System 6 is white.

    - **Two themes, switched from a View menu.** System 6 stays the default; **Plain** ([water.css](https://watercss.kognise.dev/), MIT, vendored) is the alternative - light and dark following the system preference, ordinary system type. It exists because System 6 costs two real things, and one of them started mattering: the app now renders full engine manuals, and `bard.md` is fifty kilobytes of prose in a face drawn for twelve-pixel menu labels. This is the theme for reading; the other is the one for fun.

    - The split is what keeps this from being two copies of the app. `app.css` holds the structure and reads everything through tokens; a `themes/*.css` skin holds the palette, the chrome, and the way severity is reinforced. The reinforcement is the only genuinely divergent part - inversion and rule weight where there is no colour, red/amber/green where there is - and it works because the **word** (ERROR / WARNING / OK) has been in the markup since the 1-bit theme forced it there. That decision, made under a constraint, is exactly what made a second theme cheap.

    - Applied by five inline lines in the `<head>`, because a theme settled in JavaScript after load flashes the wrong one on every visit. That duplicates the theme list, so a test asserts the two agree - and the first version of that test passed against a deliberately broken comparison, because the id also appears in the stylesheet filename. It now checks the literal being compared, and was re-verified against the same sabotage.

    - **Three theme-collision bugs, and two guards so the class does not come back.** Adding a second theme turned every hardcoded value in the shared stylesheet into a latent bug, and three had already landed. **Every sized text field collapsed under System 6**: both themes reset `input[type=text] { width: auto }` to undo system.css forcing inputs to 100%, and that reset has exactly the same specificity as `input.cmdline` - so with themes loading last it won on order, taking the terminal's command line, the reference filter and the slot box with it. Those selectors are scoped by `#panels` now, which puts them out of reach of any element-level rule a theme writes. **Every link was black in the Plain theme**, because `a { color: #000 }` sat in the shared layer and loaded after water.css. **A failing verdict rendered inverted there**, because `.verdict.bad strong` was 1-bit reinforcement living in the shared layer, and the Plain theme's reset was less specific.

    - The fix for the last two is the same and it is structural: the shared layer describes structure through tokens, and every literal colour moved into the System 6 skin where it belongs. A test now fails on any colour literal in `app.css` bar two, each allowed with a stated reason - the diagrams need a white backing in dark mode, and a modal scrim is translucent black either way. A second test fails on any width the app sets with a selector a theme could outrank. Both were verified against the bugs they describe.

    - **Two things the first pass got wrong, found by looking at it.** The vendored **Monaco is a bitmap face drawn for 9-12px**, and every monospace run in this app - filenames, paths, `make ENGINE=<engine> TERMINAL=1` - rendered thin and genuinely unreadable at the sizes those appear; monospace now uses the platform's own face, and the small sizes came back up. Chicago is the part of the look worth having, Monaco was not. The cause of the wider muddle was `.window { font-family: Inconsolata, Menlo, Chicago, Geneva }` in system.css, which beats any rule on `body` for everything inside the window - so the whole app was rendering in a monospace fallback and Chicago appeared only where it happened to be named. The window now states its font deliberately. And the **menu bar was four painted menus that did nothing** - the same dead-affordance mistake the close and resize widgets had just been removed for. File/Edit/Card are gone; the Apple menu is real, opens on focus (system.css does that in CSS, no script), and its one item opens an About box built on a native `<dialog>` so Escape and the focus trap come from the platform. A test now fails on any menu item with nothing behind it.

    - Two of system.css's defaults had to be overridden for a dense tool rather than a document: `select { width: 10rem }` (Convert puts six in a row) and `.window-pane { font-size: 18px; overflow-y: scroll }` (an inner scrollbar inside the page's own, and a very tall Reference tab). Its buttons are class-based (`.btn`), so the same border-image is applied to bare `<button>` in `app.css` instead — the markup stays semantic and the look stays system.css's own.

  - **A simplification pass, after the page was called complex and confusing — and the measurement agreed.** The landing tab asked for **121 words of reading before offering two buttons**: a lead paragraph and a four-step list ahead of the controls. The material was worth keeping (a user who trips the 12-character filename limit does need to know it is the firmware's directory scan, not a whim), so it moved into a folded `<details>` aside rather than being deleted — the answer stays one click from the question, and out of the way of everyone who did not ask. Words before the first control now read build 121 → 12, verify 57 → 10, reference 54 → 7. Reference stays dense at ~670 words because it is a table of ten engines, but filtered to one engine — which is what the filter and chips are for — it is 44; slot lists are abbreviated as `sk_card.py layout` already abbreviates them, and the firmware citations sit behind a checkbox, since "prove it" is a developer's question on a screen aimed at somebody filling a card.

    - Three further reductions in the same pass. **The tab bar now groups**: Build/Convert/Verify are the job, and Reference (a lookup) and Terminal (needs a firmware build almost nobody has) are pushed right — five equal-weight tabs read as five decisions to make before starting. **Six competing boxed styles became a hierarchy**: `.callout` dropped to a quiet left rule so it stops fighting `.verdict` and `.finding`, which respond to something the user just did, and a test now fails on any CSS class the views never wear (`.steps` had already outlived the tab it was written for). **`js/terminal/` collapsed into one `js/terminal.js`** — four files and a directory level to hold ~550 lines that only ever run together; the boundaries that actually earn their keep are kept as sections, and they are real ones (framing is transport-free, `Device` takes any transport, which is why both are testable with no port).

  - **A Home control in the menu bar.** The menu bar is now `Home | Engines | View`. It is a button rather than a menu, because "go back to the start" has exactly one meaning and a menu holding a single entry is a click of ceremony for nothing. It matters most on an engine page: that route deliberately selects no tab, so without it the way back was a guess. It returns to the landing tab and drops the fragment, so the URL is the bare page again rather than `#engine/<name>`.

  - **Two things the browser pass was going to find, found and fixed first.** **The drag-and-drop path was silently dropping files**: it read `dt.items` *after* awaiting `getAsFileSystemHandle()`, and the browser invalidates the drag data store once the drop event's task ends - so a single loose file dropped on Verify returned nothing, with the dropzone reporting zero files and no error. Every read of the store now happens synchronously before the first `await`. The test drives it with a transfer that expires the way a real one does, and was checked against the old ordering to confirm it fails there. **The tablist declared `role="tablist"`/`role="tab"` and implemented none of what those roles promise** - no arrow-key movement, no `aria-controls`, and all five tabs in the page's tab order rather than one. That is worse than plain buttons, because assistive tech is told a contract the page does not honour. Now a roving `tabindex`, arrow/Home/End movement with wrap, and panels and tabs that name each other; the key logic is a pure function so it is tested directly rather than through a shim that cannot model focus.

  - **Two dead ends closed on the serial path**, both found by reading the connection code rather than by running it, and both now covered against a scripted port. **An empty port chooser is no longer silent:** the picker is filtered to the Daisy's USB vendor id, which is the right default, but WebSerial reports a chooser that matched *nothing* and one the user *cancelled* as the same `NotFoundError` — so the previous `return` meant a user whose board fronts the CDC endpoint with a different bridge got an empty dialog followed by no message at all, which reads as a broken tab. The console now names the vendor id it filtered for and reveals a "List every serial port" retry. **A port that goes away is no longer ignored:** `SerialTransport` gained an `onClose` notification, because the read loop simply ending left the tab claiming a live connection, the command line enabled, and the CPU poll firing commands that timed out three seconds at a time. A deliberate `close()` is excluded, or the teardown re-enters itself.

  - **The offline cache is checked against the app it ships with.** `sw.js` lists its assets by hand and nothing else knew that list existed, which invites a one-sided failure: add a module, forget the list, and everything passes — every test, every online visit — because the network answers. It breaks only offline, only for users who already installed the worker, and only on the tab that needed the missing file, which is exactly the audience the worker was added for. The list is now asserted against a filesystem walk in both directions (a shipped file that is not cached; a cached file that no longer exists, which matters because `addAll` is atomic and one 404 silently leaves the app with no offline support at all), plus an import-graph walk from `index.html`'s entry point that catches a module nothing imports. All three were confirmed to fail on the drift they describe before being kept.

  - **Verified host-side only.** The suite covers the format writers, the checker, the builder, the ZIP writer, the terminal client and its framing, and mounts all four views under a minimal DOM shim — which caught one real bug (`'serial' in navigator` is true on a browser that declares the property and leaves it undefined, hiding the "this browser cannot do it" notice behind a Connect button that then throws). **Not** verified: no real browser has loaded the page, no real card has been read, and no device driven over WebSerial — [`docs/dev/web-frontend-checks.md`](docs/dev/web-frontend-checks.md) turns that gap into a mechanical half-hour pass with exact steps, expected output and the CLI command to diff each result against. Reading the code to write it turned up two things to target: the drag-and-drop path reads `dt.items` after an `await`, by which point the drag data store may have been invalidated (so a dropped loose file may silently yield nothing), and the service worker registers only over HTTPS, so the offline check cannot run against `make web-serve` at all. Note also that released firmware has **no** terminal (`build_release.py` never passes `TERMINAL=1`), so the Terminal tab serves people who build their own images; whether to ship terminal-enabled releases is an open firmware decision, unchanged by this work.

- **SD card tooling: a base card, a front-end for the converters, and a diagnostic that explains a broken card** (`scripts/sk_card.py`, `card_layout.py`, `card_audio.py`; TODO P1.5). The project shipped firmware for ten card-reading engines but no card and no way to check one. Between them those engines use **nine folder layouts and four incompatible audio formats**, and because the firmware converts nothing on the audio path, every way of getting it wrong fails *silently*: a wrong-format file is reinterpreted as garbage rather than rejected, a filename over 12 characters is skipped entirely by the directory scan, a file under 32 KB is skipped too, and `config.txt` written as `key=value` parses as nothing. The device's only feedback is an LED.

  - **`verify`** re-implements the firmware's own checks — the WAV chunk walk from `raw_stream.h` (including the `WAVE_FORMAT_EXTENSIBLE` unwrap and the 64-chunk bound) and the scan rules from `stream_deck.cpp` — and reports each violation with its fix, exiting non-zero. It deliberately does **not** use the `wave` module, which rejects extensible-format files the firmware accepts and so would disagree with the device on exactly the files users get wrong.

  - **`init` / `make sdcard`** builds a complete card — every folder, `config.txt`, `BARD.CFG`, `radio/rate.txt`, the `examples/{chuck,csound}` patches, and a `README.TXT` **in each folder** restating that folder's rules — then packages it as `dist/<version>/sk-card-<version>.zip`, which `make gh-release` ships automatically. Demo audio is **synthesized, not sampled** (tones, sweeps, noise beds, patterns, formant babble), so every engine makes a sound on a fresh card with no licensing questions and no download weight. Stdlib-only and byte-reproducible (fixed ZIP timestamps), matching `build_release.py`'s no-venv constraint on the release path.

  - **`convert`** dispatches user audio to the right folder and format, choosing a decoder per file: cysox (in-process libsox) where that build has a handler, else ffmpeg, else the sox binary. The per-file probe (`cysox.sox.find_format`) is not fussiness — libsox format support is a build-time property and mp3/flac are commonly absent (`libsox-fmt-*` are separate Debian packages), so a fixed preference would fail on most real input.

  - `scripts/test_sk_card.py` adds 66 tests, including a **firmware-parity** group that reads the C++ and fails when a mirrored constant (the 32 KB floor, the 12-char limit, bank capacities, the path literals, the 44-byte header) moves — the one way this tooling could silently go wrong is by validating against rules the device no longer has. New docs at [`docs/sd-card.md`](docs/sd-card.md).

- **CPU load is readable over the terminal (`query cpu` / `cpumin` / `cpumax`, `reset cpu`) — hardware-verified.** The platform has always owned a whole-callback `CpuLoadMeter` (`src/meter.h`), but reading it meant `METER=1`, which brings up a **second USB device** purely to print the numbers — on the same OTG core the terminal needs. `METER=1 TERMINAL=1` was therefore not a build that could exist: the numbers and the channel that would collect them were mutually exclusive. Measuring is cheap (two `System::GetTick()` reads per block), so a `TERMINAL=1` build now drives the meter itself and reports on request; `app.cpp` gates the measurement on `SPK_CPU_METER` (`METER || SPK_TERMINAL`) and keeps the USB-printing block under `METER` alone. Three `Float` queries rather than one `Text` line, so replies are the bare `ok <value>` the existing sweep already parses; values are percent of the block budget. `reset cpu` bounds the measurement interval — without it `max` is whatever the boot transient was. Zero cost when off (`cpu_stat.o` is 0 bytes; with neither flag the guard is undefined and `ProcessAudio` is unchanged). ([`docs/dev/terminal-dispatch.md`](docs/dev/terminal-dispatch.md) "CPU load")

- **Liveness masks completed — every engine now reports a real one** (extends the eleven-engine entry under 0.6.1). The three **generated** families derive their masks rather than listing them, because a hand-written mask would be a second copy of a table the generator owns and would drift on the next regeneration: `FaustEngine`/`FaustChainEngine` (chorus, filter, voice) from `Role::bound()` — which `ParamId`s actually captured a kernel slider at `init()` — with the chain wrapper taking the **union** of both stages, since they bind different tables; `GenEngine` (gigaverb) from `W::index_of(id) >= 0`, now a documented member of the wrap contract. The rest are hand-written against their `set_param` switch: edrums, graincloud (granular's list plus `Aux`/`AltPos` for the cloud layer), reso, mosc, csound, chuck. `passthrough` is deliberately **empty** — it overrides neither `set_param` nor `set_config`, so the inherited all-live default was advertising 24 params on an engine that has none, and a sweep would have written all of them and read back zeros.

- **`make test-hw` has now run against real hardware — `30 passed, 3 skipped`.** Previously only ever run device-less, where it skips cleanly and therefore proved nothing. Confirmed live on `pstretch TERMINAL=1`: `describe` reports `masked=True` with 9 params advertised instead of the whole 24-id enum, so the sweep tests only what the engine implements — the stated blocker on this target is gone.

- **First measured CPU numbers for an engine (pstretch), and a method for the rest.** `reset cpu` → drive → `query cpu`/`cpumin`/`cpumax`, sampled repeatedly. `WINDOW=8192` runs ~41.5% avg with a peak still **climbing** through the sample (87.6 → 91.3%, not converged — so its true worst case is unknown and worse); `WINDOW=4096` runs ~33.3% avg with a peak converged within 0.3 pp (~63.6%). Both passive, not driven hard. Reading `min` (~2%, the platform floor with the DSP idle) alongside avg/max separates "expensive" from "spiky"; re-reading `max` after one reset reveals whether it has converged, which is the main reason the reset verb earns its place. ([`docs/engines/pstretch.md`](docs/engines/pstretch.md))

### Changed

- **The front door.** `README.md` opens with a four-step quickstart — get a binary, flash it over the rear USB-C, build a card if the engine reads one, make a sound — above the engine catalogue rather than 250 lines below it. The material all existed; it was ordered for someone who already knew the project.

- **`docs/manual.md` is now the *platform* manual, and granular's control reference moved to [`docs/engines/granular.md`](docs/engines/granular.md).** The document was titled as the Spotykach manual and read as though granular were *the* instrument — a structural mismatch for a project of 21 interchangeable engines, and doubly odd because granular is the one engine releases deliberately do not ship. What stayed is what is genuinely shared: power, clock and sync, CV/gate, routing, the card, `config.txt`, MIDI, flashing — plus a table routing the reader to their engine's page. What moved is the granular-specific half: decks and modes, recording, the knob table, effects, modulation, the sequencer, and the notes-vs-published-manual list. A few platform sections still described granular behaviour as if universal (the Mix CV, the gate outputs, generative-stereo panning); those now say which parts are the engine's choice.

- **P5 resolved: keep both build systems, Makefile canonical.** The item framed this as binary — finish the CMake adoption or back it out — because three build files with a hand-duplicated engine list drift silently, and had. CI now builds six flag-sensitive engines through CMake alongside the full Makefile matrix, so the drift is caught mechanically; the liability was never having two build systems, it was having no forcing function. The decision, what it gives up (the compiler-enforced boundary was CMake's headline justification and is not being built), and what would reopen it are in [`docs/dev/cmake-gap.md`](docs/dev/cmake-gap.md#decision-2026-08-08-keep-both--makefile-canonical). It was conditional on arming CI's automatic triggers, done 2026-08-11.

- **Documentation sweep.** Seven broken links: `docs/architecture.md` pointed at three dev docs retired when the platform/engine refactor completed (six references), and `chuck-midi-in-porting.md` named a file that had been renamed. Also dropped a `CLAUDE.md` reference from `manual.md` (no such file), removed a stray tool-call artifact (`</content> </invoke>`) from the end of `chuck-midi-in-porting.md`, corrected `architecture.md`'s "186 KB `SRAM_EXEC`" to the post-`993210f` 300 KB split with `pstretch`'s exception noted, and gave `manual.md` a scope note — it is titled as the Spotykach manual but documents the granular engine, which is the one engine releases do *not* ship.

- **The opt-in CMake path had drifted out of parity with the Makefile** (TODO P5; the full analysis is in [`docs/dev/cmake-gap.md`](docs/dev/cmake-gap.md)). Nothing forces the two to agree — CMake is opt-in, so a Makefile-only change breaks nothing anyone notices until someone builds that engine the other way. Seven divergences had accumulated, two of them hard build failures:

  | gap | effect |
  |---|---|
  | `src/terminal/*.cpp` missing from the source list | **any `TERMINAL=1` build failed to link**, on the whole Terminal/dispatch surface |
  | pstretch not on `linker/alt_sram_pstretch.lds` | **`region SRAM overflowed by 80584 bytes`** — pstretch did not build |
  | reverb missing `OPT = -Os` | linked, but ~6 KB larger than canonical |
  | csound missing `--wrap=aligned_alloc` | the wrap keeping nano-libc's `aligned_alloc` from pulling `posix_memalign`, absent under nosys |
  | `USB_MIDI` had no CMake equivalent | **the QSPI engines silently lost device MIDI** — builds, boots, ignores MIDI |
  | `TERMINAL` `USBDIAG` `TERMPORT` `METER` `WINDOW` `BRINGUP` `NOCHUCK` `CHUCKLVL` | unimplemented; passing them did nothing |
  | `SOFTCUT_EXTRA` | unimplemented |

  `USB_MIDI` needed a stand-in for the Makefile's `APP_TYPE`, since `BOOT_SRAM`/`BOOT_QSPI` differ only by linker script here; an explicit `ENGINE_BOOT_QSPI` flag on the three QSPI engines now drives the default. The worst of these is the MIDI one: it produces an image that builds, boots and runs, and is only wrong on a bench.
  - **Toggles could not simply be forwarded.** A CMake cache entry is sticky, so `-DTERMINAL=1` once would make every later build in that directory a terminal build — and the old frontend configured only when `CMakeCache.txt` was absent, so a changed toggle was ignored outright. `Makefile.cmake` now configures on every invocation and passes every toggle with its current value, empty included; empty is falsy to `if()`, which reproduces make's absent-means-off. A warm reconfigure costs ~0.1 s.

  - This is also the one place CMake is structurally **better**. `SPK_TERMINAL`, `TERM_USBDIAG` and `SPK_TERMINAL_PORT_EXTERNAL` change *type layout*, so a partial rebuild yields members at wrong offsets — a frozen panel with a working terminal, which cost a hardware session to diagnose. The Makefile defends with three stamp files and an object wipe because GNU Make 3.81 compares mtimes at whole-second resolution; CMake records the definitions in `flags.make`, which every object already depends on.

  - **`Makefile.cmake`'s `FAUST_KERNELS` copy had drifted too**, listing three kernels where the Makefile lists eight (missing `greyhole`, `chorus`, `filter` and both `voice` stages), so `make -f Makefile.cmake faust-gen` regenerated under half of what `make faust-gen` does — and succeeded doing it. Of the six host-side variables that file mirrors, this was the only one adrift.

  - The Makefile's own `$(error Unknown ENGINE ...)` list was stale as well, naming 18 of 22 engines. Both messages now name the same 22. It drifts because it is duplicated and hand-maintained, and a shared variable would not fix it: `gen_engine.py` and `gen_faust_engine.py` anchor on the literal `"else\n$(error Unknown ENGINE"` and add a branch without touching any list — so nothing may be inserted between the `else` and the `$(error)` line.

  - **Verified:** all 22 engines build under both systems, and the size delta is now uniform across the entire published set — 18 of 18 engines, range 1164–1548 B, mean 1290 B, mode exactly 1256 B, `.bss` matching everywhere. That mode is the known newlib residual measured exactly (`.text` +272, `.data` +972, `.init_array` +4). Symbol-level diff on one engine: **1426 of 1428 common symbols are size-identical**, and every symbol that differs or is unique to one build is newlib. The binaries are *not* byte-identical and cannot cheaply become so — 358 of 894 common `.text` symbols sit in a different order, because the two systems feed objects to `ld` in different sequences.

- **`chorus` did not build at all**, in either build system — `region 'SRAM' overflowed by 98808 bytes`, at 145.52% of the 212 KB data region. Same class of breakage as the `pstretch` one found earlier: commit `993210f` moved 114 KB out of `SRAM` into `SRAM_EXEC` so the terminal channel would fit everywhere, and `chorus` was never rebuilt afterwards. Unlike `pstretch`, though, the answer was not another linker script.

  - The cause was one symbol. `chorus.dsp` used `os.osc` for its two LFOs, and `os.osc` is `os.oscsin` — an `rdtable` over the platform library's `tablesize`, which is `1<<16`. That compiles to `static float ftbl0mydspSIG0[65536]` — **262144 B, 83% of the engine's entire `.bss`** — for two oscillators that never run above 5 Hz. It is a static at namespace scope rather than dsp-object state, so the arena placement-new that keeps the rest of a Faust kernel out of SRAM cannot touch it.

  - Fixed in the `.dsp`, where it originated: `os.m_oscsin` is the same sine of the same phase (`lf_sawpos : *(2*ma.PI) : sin`) computed rather than tabulated, with no table at all. `SRAM` fell from 145.52% to **24.76%** on the stock `alt_sram.lds` — no per-engine linker script, and the residue matches the predicted 53752 B exactly, confirming the table was the whole overflow. It is also *better* than a smaller table would have been: these LFOs modulate a fractional delay, where a table-quantized control signal is audible as zipper noise, and a computed sine is continuous. The cost is two `sinf()` per sample, estimated at ~1.6% of one core at 48 kHz — **not yet measured on hardware**; confirm with `query cpu` in the P2 session.

  - Audited every other Faust kernel in the tree for the same pattern: none has a static table, so `chorus` was the only instance. The kernel was rebuilt with an explicit `FAUST_KERNELS=src/engine/chorus:fx_:chorus` to regenerate only this engine — the local libfaust is 2.85.9 against the 2.85.5 the others were built with, and while the generated code is byte-identical, a full `faust-gen` would have churned the version banner in seven unrelated kernels.

- **`pstretch` did not build at all** — a regression from the `SRAM_EXEC` rebalance below, which shipped unnoticed because that change was validated per-engine on `SRAM_EXEC` only. Not "cannot host the terminal": no link at any window, with or without it (`region SRAM overflowed by 80576 bytes`). Moving 114K from data `SRAM` into `SRAM_EXEC` (186K → 300K) took exactly the region pstretch lives in — its FFT working set is **297664 B** of fixed-size arrays dimensioned by the compile-time `kWindow`, nothing dynamic, so the region either fits it or the link fails. Fixed with a per-engine linker script, `linker/alt_sram_pstretch.lds` (200K/312K), selected automatically on `ENGINE=pstretch` so a plain `make ENGINE=pstretch` is correct too; the other 20 engines stay on 300K. Same precedent as `linker/alt_qspi_chuck.lds`. pstretch is the one engine large in **both** halves at once, so only a narrow band satisfies it — 186K overflows code, 210K overflows data. All three images flashed and confirmed working. `WINDOW=4096` does **not** remove the need for the script (still 44928 B short at 300K); it widens the viable band from ~19K to ~67K. **The lesson: when a change trades one region against another, check the engine with the largest total, not the largest code.**

- **`softcut` was missing from the SD card layout entirely**, so both front-ends were blind to it: `sk_card.py init` never created `softcut/`, `verify` classified `softcut/loop_a_1.wav` as "belongs to no engine" and skipped it, and `convert` could not target it — while the engine shipped in every release. It adds **no new format**: softcut writes 48 kHz mono 32-bit float WAV through the same `StreamDeck` path as tape, so it reuses `FMT_TAPE` outright. Only the folder and the `loop_` prefix differ, and that separation is structural — it is what stops a softcut loop overwriting a tape take, and it is how `verify` knows to apply softcut's ~10.9 s buffer cap (`kBufFrames = 1<<19`) to bytes that are otherwise identical to tape's. The base card now carries a softcut demo loop, so "every engine makes a sound out of the box" holds again.

  - The real fix is the guard: `test_every_card_reading_engine_has_a_bank` derives the card-reading engine set from the firmware source and fails on any engine without a bank. It searches **both** access mechanisms — the streaming service *and* the platform tape/slot path in `hw/card.cpp` — because matching only the first would have missed `granular` and `graincloud`. An engine that legitimately reuses another's folders must be declared in that bank's `also_read_by`, making the sharing an explicit claim rather than an absence nobody notices; `graincloud` is the one entry today. A second test guards the guard, failing if the firmware renames those APIs so the search quietly matches nothing and the first test passes while checking nothing at all.

  - Knock-on: the layout is now **nine** folder layouts, not eight. Corrected across the docs, and the web app's copy now *derives* both that count and the format count from the layout data, with a test asserting the view hardcodes no such number — prose does not have a test, which is why the figure went stale in the first place.

- **`SK/{B,G,P,R,T,Y}` is now recorded as the platform's *shared* tape store, not granular's folder** (`Bank.also_read_by` / `Bank.readers`). It reads like granular's private directory and is not: `kRootDir = "SK"` lives in `src/memory/storage.cpp` and serves every engine declaring `CapTapeStorage`, which today is granular **and graincloud**. That distinction is structural in two directions — renaming the folder to `granular/` would have broken graincloud (which ships) plus every card the stock upstream firmware ever wrote, and conversely granular's absence from the published engine list does *not* orphan the base card's demo tapes, because graincloud reads them. Both facts are now pinned by tests instead of being rediscovered; the web app's folder table names every reader of a folder rather than just the owning engine, and labels the set as `SK/{B,G,P,R,T,Y}` rather than the misleading range `SK/B .. SK/Y`.

  - The release-consistency guard is per *folder*, not per engine: `test_every_card_bank_is_playable_by_a_published_engine` fails if the base card carries content in a folder no published binary can open. `granular` therefore stays deliberately unpublished (it is the upstream firmware, not something new to this fork) without the card contradicting the binaries beside it.

- **A device error during pytest collection aborted the entire hardware run.** `tools/test_generic.py` opens the `Device` at *module* level (the `parametrize` decorator calls it) and caught only `Timeout`, so anything else — here a permission-denied port — became a collection error that killed the whole session, including `test_descriptor.py`, the one file needing no device. Both it and the `device` fixture now also catch `OSError` (pyserial's `SerialException` subclasses it), and the fixture skips naming the actual reason rather than the generic "no device attached" — a silent skip is indistinguishable from having no hardware. The documented "no-ops safely without hardware" guarantee now covers a port that exists but will not open: permission denied, or one already held by `skterm.py`/`screen`. (Operational note: the port is `root:dialout` 660; `sudo usermod -aG dialout $USER` does **not** affect already-open shells — use `newgrp dialout` or re-login.)

- **The four linker scripts moved to `linker/`.** `alt_sram.lds`, `alt_qspi.lds`, `alt_qspi_chuck.lds` and the new `alt_sram_pstretch.lds` were accumulating in the repo root. Both consumers take a plain relative path (`-T$(LDSCRIPT)`; `${CMAKE_CURRENT_SOURCE_DIR}/${ENGINE_LDSCRIPT}`), so this needed only correct strings — updated across `Makefile`, `CMakeLists.txt`, `pod/Makefile.chuck`, `scripts/build_release.py` and its test, four source comments and nine docs, including the copy-pasteable `LDSCRIPT=` lines in Makefile comments. Historical references in this file are left as they were.

- **`docs/dev/terminal-impl.md` corrected: "every engine now fits" was true only of `SRAM_EXEC`.** pstretch fails on the data region instead, which the fit table was not measuring.

### Fixed

- **`describe` advertised the wrong address for the four platform reads.** `docs/dev/terminal-osc.md` files `cpu`, `cpumin`, `cpumax` and `usb` under the platform section — `/sk/dev/cpu`, "they report on the channel and the board, not on the engine's control surface" — and `osc_addr.cpp` resolves them there. `describe_state_rows` did not: it walked the whole platform query table and composed every row with the `state` kind, so the descriptor advertised `/sk/state/cpu` for a read the resolver answers at `/sk/dev/cpu`.

  Nothing was broken by it, which is why it survived a hardware-verified release: **both spellings resolve**, so a host binding to either one works. That is also what made it invisible — `test_terminal_osc.cpp`'s drift check walked `param` rows only, and would have passed on state rows anyway, because it asked whether an advertised address is *reachable* rather than whether it is the address the rules predict.

  The list of four now lives in one `is_platform_read()` predicate that the resolver and `describe` share, rather than inline in `do_dev()` alone; only the platform table is eligible for the `/sk/dev/` spelling, so an engine that declares a query called `cpu` still gets its own address. Three checks close it: the drift loop covers `state` rows as well as `param`, it asserts the exact spelling of all four, and `host/test_osc_addr.cpp` compares the entire advertised set against the host-side model.

  Found by that last test on its first run. The unmasked descriptor bundle is 16 B smaller as a result (5548 → 5532); the `kOscBundleCap` note recording that measurement was restated, having also gone stale earlier when the state row gained its fourth string.

- **A device that opened but did not answer aborted the entire `make test-hw` session.** `tools/test_generic.py` parametrizes its sweep from a `describe` taken at *collection* time, and its own docstring warns that anything raised there is a pytest collection error which "aborts the entire session — including test_descriptor.py, which needs no device at all". It then guarded the `Device()` constructor and **not** the `describe()` call after it. So the one failure mode the guard existed for — a port that opens and stays silent — was the one it did not cover, and it took down every unrelated test with it.

  Found by pointing the line client at an OSC build, which is exactly that shape. The collection-time describe now lives in `tools/harness.py`, guarded against any failure and cached including the failure, so hardware trouble degrades to skips as intended. It is also codec-aware, which is what lets `--osc` reach the parametrized sweeps and not just the fixtures.

- **`Device.pad()` discarded its reply.** It returned `None`, so `pad play`'s `empty=<0|1>` — the one thing a pad press reports, and a value a host has no other way to obtain — was unreachable from the client. Now returns the payload, and `tools/test_generic.py` asserts it. Deliberately asserts only the *reply*, not that playback started: tape's `on_play_pad` returns a constant and it declares no transport query, so a "the deck is now playing" assertion would be unfalsifiable — which is precisely how the original `test_tape.py` came to assert something (`pad rec A` makes a deck non-empty) that had never been run and could never have passed.

- **`SynClock::_external_clock` was never initialized** — the same defect as the `Divider` one below, in the same subsystem, found while writing the granular audio test. Declared in `synclock.h`, absent from the constructor's initializer list, no default member initializer.

  On target it was masked by `.bss` zeroing (the only `SynClock` lives inside the file-static `AppImpl`). Off target it was indeterminate, and when it read true the clock believed it was slaved to an external source — in which case `Run()` merely *arms* (`_is_about_to_run`) and `Tick(false)` never emits. **Measured: 0 ticks in 3000 blocks.** So nothing clock-dependent — the granular Slice mode, the step sequencer, key/bar boundaries — had ever been exercisable off-target, and the `transport.tick(false)` calls in `test_engine_params.cpp` had been doing nothing at all. After the fix the same probe reports 576 ticks in 6 s, exactly right for 48 PPQN at 120 BPM. All four bool members now carry default member initializers as well as constructor ones.

- **`plaits/user_data.h` did not compile off-target.** The host harness defines `-DTEST` for an unrelated reason — it selects stmlib's portable C paths over the ARM `ssat`/`usat` assembly — and that flag also selected upstream Plaits' mock-flash branch, which calls `printf` without including `<cstdio>`. Rather than add the include and enable a mock this project never wants, the project's own `PLAITS_USER_DATA_STUB` now wins when both are defined, so the host test takes the same branch the firmware does.

- **`Divider::_triplets_on` was never initialized.** It is absent from the constructor's initializer list and had no default member initializer, so it was read indeterminate — and when it read true, `tick()` rescaled the entire grid by 0.667 (an 1/8 grid at 48 PPQN firing every 16 pulses instead of every 24). This is not a corner: `Divider` drives the platform `Transport`'s tick grid, which every tempo-synced engine observes, and both granular decks' pattern sequencers.

  On target it was benign **by accident** — every `Divider` in the firmware lives inside the file-static `AppImpl`, so `.bss` zeroing supplied the false. Correctness rested on where the object happened to be allocated: any automatic-storage `Divider` got garbage, which is exactly what the host tests construct, and why `test/test_divider.cpp` had been failing 7 checks. That suite is now 116/116, the count the README always claimed. All three bool members now carry default member initializers as well as constructor ones.

  Noted while fixing it: `set_triplets_on()` and `set_swing()` are **called from nowhere in the firmware**. Both are implemented, integrated into `tick()` and tested, but no gesture reaches either. They are kept (the delay/qdelay division tables already advertise triplet divisions, so the grid maths is worth keeping correct) with the situation recorded in `src/dsp/divider.h` and `TODO.md`.

- **`graincloud` had become a byte-for-byte fork of `granular`** — 35 of 42 files identical, ~3,400 duplicated lines. Fixes to `granular/` silently did not reach it, the two had already begun to drift, and `graincloud` is the *published* one of the pair while `granular` is not: the shipped binary was the copy receiving fewer fixes.

  The fork was itself a regression. `docs/dev/graincloud-impl.md` already described the intended design — "graincloud is **the granular engine compiled with `SPK_GRAIN_GF`**" — and that design is now restored. The copy is deleted; `src/engine/graincloud/` holds only `gf_cloud.{h,cpp}`, `graincloud_engine.{h,cpp}` and the vendored GrainflowLib. The three places the two engines genuinely differ carry `#if SPK_GRAIN_GF` blocks: `generator.h` (the `GfCloud` handle and the Play-pad gate), `generator.cpp` (the cloud replaces the Vox loop in `process()`), and two lines of `deck.cpp`.

  **`src/engine/granular/` did not move.** A shared `graincore/` directory was the obvious refactor and is the wrong one here: that tree is a deliberately frozen copy of the upstream Synthux Academy Core, kept diffable against it, and relocating it would turn every future upstream diff into a rename-plus-edit. Guarding three files that were *already* divergent copies costs ~30 lines of clearly-marked diff noise — strictly less upstream burden than the 3,400 duplicated lines it replaces.

  Consequence worth knowing: both engines now compile the same sources to the same object basenames with different flags, and `SPK_GRAIN_GF` **adds members to `Generator`**, so a `granular`↔`graincloud` switch must invalidate every object or the next build has members at the wrong offsets. `build/.grainflavor-stamp` does that, in the same idiom — and for the same reason — as the `TERMINAL`/`USBDIAG` stamps. Verified by building both ways and back; the graincloud image is the same size as the published `0.6.1` binary.

- **`host/test_graincloud` did not compile, and was excluded from `make -C host test`** — so a full-engine test existed, was dead, and nobody knew. `gfParam.h` declares `enum class ... : std::uint8_t` without including `<cstdint>`, relying on a transitive include that arm-none-eabi happens to provide and the host toolchain does not. Fixed in `gf_cloud.h` and wired into the suite, where it now serves as the end-to-end check on the shared-source graincloud build: it writes a known DC value into each deck's buffer through the storage port and asserts the cloud output scales with it.

- **Two incremental-build faults, both found by rehearsing the new CI matrix locally in a single build tree.** Neither is reachable in CI itself (each job gets a clean runner), and both break the README's promise that switching `ENGINE` needs no `make clean`:

  - **`reso` → `mosc` failed to link.** Rings and Plaits are the same author's codebases and reuse file names, so `resources.cc`, `dsp/resonator.cc` and `dsp/string.cc` exist in both and compile to the same three object basenames. A switch reused the previous engine's objects and the link died on `undefined reference to plaits::lut_sine` — which reads like a missing source rather than a stale object, and is why `make engine-mosc` opens with a full `make clean`. Declaring the objects out of date is *not* enough: the stale `build/<name>.d` still names the other engine's source as the first prerequisite, and the pattern rule compiles `$<`, so make rebuilds the object from the wrong file. `build/.engine-stamp` now deletes the colliding objects **and their `.d` files**; the list is derivable with `find src -name '*.cc' -o -name '*.cpp' | xargs -n1 basename | sort | uniq -d`.

  - **An SRAM → QSPI switch silently produced an image with no device MIDI.** `USB_MIDI` defaults on for `BOOT_QSPI` and off otherwise, so the platform objects carried over from an SRAM build lacked `-DSPK_USB_MIDI`. The image builds, links, boots and ignores MIDI — the identical failure mode recorded below for the CMake gap, reachable from the canonical Makefile as well. A clean `mosc` is 304,716 bytes; the same build over a stale `reso` tree was 292,552. Now stamped like the other invisible flags (`build/.usbmidi-stamp`, full object wipe); clean and incremental images are byte-identical in both directions.

- **The committed `web/` export depended on filesystem traversal order.** `sk_card.verify_card` walks with `os.walk`, which sorts filenames but not subdirectories, and the exporter sorted `files` and `dirs` but not `findings` — while `scripts/test_web_export.py` compares the committed fixture to a fresh export **byte-for-byte**. The result is a test that can fail on a different machine with byte-identical code, and which was failing on the committed tree. Sorted at the walk (which also makes `make check-sdcard` output stable and diffable) and again in the exporter.

## [0.6.1]

### Added

- **On-target engine testing over USB-C (`TERMINAL=1`) — hardware-verified.** A bidirectional text/command channel on the rear USB-C CDC port lets a host script drive an engine into a known state, exercise a feature and assert on a property read back, with no knobs, patch cables or MIDI gear. The whole `IEngine` input surface is addressable by name (`set param`, `config`, `cv`, `gate`, `midi`, `pad`, `fx`), state is readable (`query empty|mix|route|gateout|usb`), and `mode test` freezes the physical input path so injected stimulus is the only thing driving the engine. `describe` emits the device's own control surface so a host configures itself instead of hardcoding per-engine knowledge. Zero cost when off - the terminal TUs compile to 0 bytes and the default binary is unchanged. ([`docs/dev/terminal-control.md`](docs/dev/terminal-control.md), [`docs/dev/terminal-impl.md`](docs/dev/terminal-impl.md))

- **`make test-hw` — the on-target counterpart to `make test`.** A pytest harness (`tools/`) plus a `skdev` client library and `skterm.py` REPL drive a flashed device over serial. `test_generic.py` is driven entirely by `describe`, so one file sweeps every engine build; with no device attached the run skips cleanly, so it is safe in a hardware-free pipeline. First real runs: delay's and qdelay's six declared params round-tripped across both decks. ([`docs/dev/terminal-tools.md`](docs/dev/terminal-tools.md))

- **L1 state-query coverage in the generic sweep.** Two further describe-driven tests, again with no per-engine knowledge: every advertised query must answer on every deck its scope claims, and any config with a same-named query must be observable through it **in the same encoding** - the automatic form of the `config route` / `query route` mismatch found by hand. `parse_describe` now keeps each query's scope.

- **Off-target coverage of everything below the USB transport.** `host/test_terminal.cpp` exercises the tokenizer, line assembler, SPSC ring, TX FIFO, value coercion, float formatting (integer decomposition - the firmware links no `_printf_float`), every verb's `IEngine` binding and exact reply bytes, the error taxonomy, and `describe`, against a recording mock engine under `make test`. It also writes the real `describe` block to `build/describe_sample.txt`, which `tools/test_descriptor.py` parses through the actual host parser - closing the firmware/host loop without a device.

- **Per-engine liveness masks (`live_params()` / `live_configs()`) on eleven engines.** `describe` lists only what an engine actually implements, and the `descr` line reports `masked=0|1` so a host sweep can skip engines still on the "all live" default rather than emitting false failures. delay and qdelay are hardware-verified; the rest are derived from each engine's `set_param`/`set_config` switch and not yet swept. granular's mask deliberately excludes `AltPos` and `Aux`, which its switch names but treats as explicit no-ops - a sweep would otherwise set them and assert on a value that went nowhere. Since the `SRAM_EXEC` rebalance every engine fits; see [`docs/dev/terminal-impl.md`](docs/dev/terminal-impl.md).

- **ABI tagging for layout-affecting build flags (`src/abi_tag.h`).** `SPK_TERMINAL` adds a member to `CoreUI` and virtuals to `IEngine`; `TERM_USBDIAG` adds another `CoreUI` member - so objects built with and without them are not link-compatible, and mixing them silently shifts member offsets and vtable slots. The affected types now live in an inline namespace whose name encodes the flags, turning that into a named link error (`undefined reference to spotykach::abi_t1d0::CoreUI::render_leds()`) instead of a runtime mystery. Verified by deliberately linking a mismatched object; costs nothing at runtime, as it only changes mangled names.

- **USB bring-up diagnostic (`USBDIAG=1`).** Renders eleven checks - clocks, HSI48, USB clock source, VDD33_USB, transceiver, D+ pullup, VBUS sensing, both D-/D+ pads' alternate function, bus reset, SOF - on the panel LEDs and as a blink code on the Daisy onboard LED, plus `query usb` as text. Built because a cased unit hides the onboard LED and SWD pads, and the channel that would normally report on itself was the thing that was down. Off by default; the build stays fully runnable.

- **`SRAM_EXEC` rebalanced 186K -> 300K (`alt_sram.lds`), which was never a hardware limit.** The H750's 512K AXI SRAM was split 186K code / 326K data; code sat at 99.29% (tape + terminal) while ~260K of the data region went unused, and softcut, bard, reverb, reso and granular could not host the terminal at all. Now 300K/212K: every engine fits with margin - worst code 69.93% (granular + terminal), worst data 68.81% (bard + terminal) - and the per-engine fit table in [`docs/dev/terminal-impl.md`](docs/dev/terminal-impl.md) is largely moot. Verified on hardware rather than only at link time: a 214812 B granular + terminal image, 24 KB past the old ceiling, boots and enumerates, so `bootloader-spotykach-v2.bin` copies well beyond the old region. The tested lower bound is ~215K, not 300K; an image approaching 300K should still be flash-tested.

- **Composite verbs: `reset` and `preset save|load <slot>`.** `reset [deck]` drives every advertised param to `IEngine::param_default()` (new virtual, 0.5 unless an engine overrides); `preset` snapshots and restores them through in-RAM slots. Both operate on exactly what `describe` advertises and reply with a count. `reset` closes a real hole in the harness - `mode test` froze the panel but left the previous test's writes in place, so tests were independent only by luck; `tools/conftest.py` now resets per test. Presets are non-persistent by design (a test snapshots many times a run and should not wear flash) and **params only**, because `IEngine` has no config getter - `set_config` is write-only. Neither applies its writes atomically with respect to the audio block; noted rather than built, since nothing has been bitten by it.

- **The rest of the `IEngine` input surface is now reachable from the terminal.** `terminal-control.md` claimed the channel drove "the full `IEngine` input surface"; it did not. Added `fx lock <flux|grit> <deck>`, `fx gritmode <deck>` (an action that returns values - the intensity/mix pair the platform uses to reseed its pickup), `seq trig|arm|clear|disarm <deck>`, and the state reads `query recorded|capacity|layout|sizetempo <deck>`. Two more are reachable by name but **deliberately not advertised in `describe`**: `query fit <deck> <fraction>` takes an argument the descriptor cannot express, and `query reseed <deck>` is a latching read that self-clears - a generic sweep would consume the flag the platform is waiting for. `set_aux_active` is left unexposed because the UI re-asserts it every `process()`. Off-target tested; not yet exercised on hardware.

- **`get param crossfade` returned 0 on six engines.** `set_param(ParamId::Crossfade)` stores the value and derives the A/B blend gains, but `param()` had no matching case in bard, glitch, pstretch, radio, shuttle, softcut or tape - the setter was copy-pasted between engines and the getter never was. Only the terminal ever reads this param back, so nothing else noticed. Found by the on-target sweep on tape, then generalised. Worth noting granular and graincloud were never affected: they return from a generic `_param_cache[id][deck]`, so a grep for the symbol wrongly flagged them and classifying by mechanism (cache lookup vs if-chain) was what gave the right answer.

- **Host client: one timeout no longer poisons a whole session.** The protocol is synchronous, so a late reply from a timed-out command was read as the *next* command's answer, and every subsequent reply stayed offset - surfacing as nonsense parse errors far from the cause, non-deterministically. `Device` now drains pending input before each command, so a timeout costs only that command; the default read timeout is 3 s rather than 1 s, since the channel is pumped from the main loop and a streaming engine scanning SD in `prepare()` stalls replies (a full sweep is ~0.1 s on delay, ~12 s on radio).

- **The generic param sweep no longer assumes exact float round-trip.** Quantized params are legitimate - tape's `aux` selects one of 8 SD slots, so 0.5 reads back 0.5625 - and the descriptor cannot declare a step. It now asserts the param **tracks input** (two targets, two distinct readings) and is a **fixed point** (writing back its own reading is stable), which tolerates quantization while still catching a missing getter, and names which of the two failed.

- **Target B built (B1): engine-specific state is now declared, discoverable and swept.** `IEngine` gains `engine_queries()` and `read_engine_query()`; an engine declares a static table (name, scope, `ValueKind`, enum labels, `safe`) and answers by index, and the platform does the matching, deck validation, reply framing and `describe` emission - so an engine writes no parser and dispatch cannot drift from description. The platform's own queries were converted to the same table, so `describe` walks one code path over both halves and a host cannot tell them apart. Wire format gains a `kind` token (`query <name> <scope> <kind> [labels]`), backward compatible - verified by sweeping a device running pre-`kind` firmware. Only `safe` (idempotent, side-effect-free) entries are advertised, which makes the generic sweep correct by construction. `CapTerminal` is now derived from a non-empty table instead of hand-set. First users: **tape** (`slot`, `loopmode`, `speed` - the varispeed actually in effect) and **radio** (`station` - which station is really streaming, so a test can assert a seek completed rather than that a knob moved). ([`docs/dev/terminal-target-b.md`](docs/dev/terminal-target-b.md))

- **Design: target B redesigned so engine-specific state can actually be used ([`docs/dev/terminal-target-b.md`](docs/dev/terminal-target-b.md)).** The `handle_command` hook shipped in phase 1 and no engine has ever implemented it - not because the mechanism is wrong, but because it is invisible to `describe`, makes every engine re-implement matching and error handling, and carries no type information, so the generic sweep cannot see it and every use would need per-engine host code. The proposal replaces the free-form read path with a declared query table (name, scope, kind, enum labels) answered by index, so dispatch and description share one source of truth and drift is structurally impossible; the platform takes over deck validation and reply framing; and engine queries emit as ordinary `query` lines, which is backward compatible and means the existing sweep covers them with no new host code. Entries are tagged **`safe = idempotent && side-effect-free`** rather than sorted into "queries" and "actions" - only safe entries are advertised, so the sweep is correct by construction. The free-form hook stays for side-effecting verbs. Unbuilt; phased with two real users (tape `loop_ms`, radio `station`) to validate it by use.

- **The command model is now written down ([`docs/dev/terminal-control.md`](docs/dev/terminal-control.md)).** Params/configs, queries and actions, the two axes that separate them (is there a stored value; does calling it change anything), and why a reply does *not* distinguish an action from a query - `pad play A` returns `ok empty=1` and `config mode A 1` returns whether it changed. What matters to tooling is repeatability, which is the sole basis for what the descriptor-driven sweep may call. Includes the three cases in this codebase where the categories leak: `set_config`'s `changed` flag, `take_param_reseed`'s latching read, and `toggle_grit_mode` reading like a getter.

### Fixed

- **The terminal was on the wrong USB peripheral.** The Spotykach's rear USB-C is wired to **OTG_HS** (PB14/PB15), not the Seed's own OTG_FS pins - as `hw/hardware.cpp` and the Makefile's unconditional `INFS_LOG_TARGET=LOGGER_EXTERNAL` both already implied. The channel had been driving two pins this board connects to nothing, which is why DFU always enumerated on a connector the app could not use. `SPK_TERMINAL_PORT_EXTERNAL` now defaults on; build a bare Seed or Pod with `TERMPORT=int`.

- **Build-flag changes could produce a silently corrupt binary.** Under GNU Make 3.81 (what macOS ships) mtimes compare at whole-second resolution, so a stamp rewritten in the same second an object was compiled did not out-date it - toggling `TERMINAL` gave a *partial* rebuild whose stale subset depended on timing. Because these flags change type layout (`CoreUI`, `AppImpl`, the `IEngine` vtable), the result was mismatched member offsets: a frozen, garbled panel with a working command channel. Stamp recipes now delete the objects outright and are prerequisites of every object.

- **TX staging buffer was reused while a transfer was in flight.** `CDC_Transmit_FS` returns OK once a packet is *queued*; in non-DMA mode the HAL copies from the caller's buffer later, from the FIFO-empty interrupt. The main loop refilled it well inside a USB frame, corrupting multi-packet replies. Now ping-ponged, swapping only on a successful transmit.

- **Dropped replies were silently swallowed**, so a full TX FIFO cost the host a whole reply and read as an unexplained timeout. Now reported as `err tx-overflow`.

- **`describe` advertised parameters that never reach an engine.** `Tempo` and `KeyInterval` live in the Transport service and `ModSpeed` arrives via `set_mod_speed()`, so a generic sweep set values that went nowhere and asserted on whatever the engine happened to store. They are no longer advertised (still addressable), which also removes the only non-normalized ranges - their declared display units were never the units `set_param` takes.

- **`config route` and `query route` spoke different encodings**, so `config route A 0` read back as `2` and route could not be round-tripped. `query route` now reports the selector encoding `config` accepts and `describe` publishes.

- **`mode test` did not freeze the panel switches.** Unlike knobs, CV and gate, switches *assert* their position every iteration, so any config the terminal set was overwritten within a millisecond - making every panel-owned `ConfigId` untestable. Now gated with the other three consult points; `mode run` hands control back automatically.

- **`mode test` had no way out if a harness died**, leaving knobs, CV and gate frozen with no recovery short of a power cycle. Now reverts after 30 s without a command (`SPK_TERMINAL_TEST_MODE_TIMEOUT_MS`); any command resets the timer.

- **`TERMINAL=1` with `USB_MIDI=1` is now a compile error.** `MidiUsbHandler` claims the same OTG_HS core as the default terminal port, and `USB_MIDI` defaults on for every `BOOT_QSPI` build - two USB device stacks on one peripheral.

- **Host tooling had never been executed and did not work.** `serial.Serial(..., dtr=True)` raises (`dtr` is a property, not a constructor keyword) so nothing could connect; `readline.parse_and_bind("tab: complete")` is silently ignored by the libedit backend macOS ships; `skdev.descriptor` could not be imported without pyserial, breaking device-free collection; and `make test-hw` invoked the system `python3`, which has neither pyserial nor pytest. All fixed, the last via a `PYTHON` variable that prefers the project venv.

- **`query usb` reported init-time state as live.** The refresh only ran under `USBDIAG`, so in a normal build the host-activity bits could never read anything but zero - the worst possible failure for a field whose job is answering "is the host talking to us".

## [0.6.0]

### Added

- **bard engine (`ENGINE=bard`) — a bookmark-navigated audiobook / storyteller, hardware-verified.** Streams long-form spoken-word books from SD (`/bard/<shelf>/`) with WSOLA pitch-preserving speed, a resume table that remembers where each book left off, auto-marks, and a room reverb. **Alt+PITCH** selects the shelf, **PITCH** the book; **SIZE** = speed, **ENV** = pitch-keep amount. Now part of the `make dist` release set. ([`docs/engines/bard.md`](docs/engines/bard.md), [`docs/dev/bard-impl.md`](docs/dev/bard-impl.md))

- **pstretch engine (`ENGINE=pstretch`) — a real-time, clean-room PaulStretch ambient time-smear, hardware-verified.** Each deck turns its input into a diffuse spectral wash (FFT → phase-randomize → overlap-add; slowing or freezing the read head stretches time without limit). SIZE = stretch 1×–64×, POS = diffusion, PITCH = ±1-oct grain shift, ENV = tone, MIX = dry/wet, Play = freeze; the per-deck Mode switch picks the Live / Capture / SD-file source. A clean-room MIT reimplementation with a vendored FFT (no CMSIS-DSP). ([`docs/engines/pstretch.md`](docs/engines/pstretch.md), [`docs/dev/pstretch-impl.md`](docs/dev/pstretch-impl.md))

- **pstretch: 8192-sample default window + per-deck modulation, CV, and gate — hardware-verified.** The default analysis window is now 8192 (~171 ms) for a lusher wash (`WINDOW=4096` for the lighter one). Adds a per-deck mod LFO (tempo-lockable) routable to diffusion / stretch / tone, additive CV inputs (V/Oct, Size/Pos, Mix, Crossfade), a gate in (re-grab in Capture, freeze-toggle in Live/SD), and CV/gate out — so pstretch doubles as an LFO / tempo-synced clock source. All off by default. ([`docs/engines/pstretch.md`](docs/engines/pstretch.md))

- **glitch engine (`ENGINE=glitch`) — a dual-deck lo-fi / circuit-bent noise voice, host-tested (hardware pending).** Each deck runs one of 12 curated algorithms ported from Rob Scape's [Noisferatu](https://github.com/rob-scape/noisferatu) (buffer glitch, logic-noise, generative blips, rhythmic noise) — the digital-glitch aesthetic the platform's cleaner engines don't cover. Alt+PITCH scrolls the algorithm; SIZE/POS are its two params. **GPLv3** (inherits Noisferatu's license), unlike the MIT rest. ([`docs/engines/glitch.md`](docs/engines/glitch.md), [`docs/dev/glitch-impl.md`](docs/dev/glitch-impl.md))

- **qdelay engine (`ENGINE=qdelay`) — a dub/ambient flavor of the delay, hardware-verified.** Keeps the delay engine's whole control grammar and swaps the character palette to **Clean / Diffuse / Duck**: Diffuse smears repeats through an 8-stage allpass into a reverb-like wash, Duck attenuates the wet while you play so repeats bloom in the gaps. Adds `src/dsp/diffuser.h` (a JUCE-free port of qdelay's Diffusor). **GPLv3** (derived from qdelay), unlike the MIT rest. ([`docs/engines/qdelay.md`](docs/engines/qdelay.md), [`docs/dev/qdelay-impl.md`](docs/dev/qdelay-impl.md))

- **delay: Reverse on the Rev pad.** The delay engine's Rev pad now toggles a per-deck click-free reverse read that composes with all characters and topologies; the play-pad LED shows cyan when reversed. Hardware-verified. ([`docs/engines/delay.md`](docs/engines/delay.md), [`docs/dev/delay-impl.md`](docs/dev/delay-impl.md))

- **softcut engine (`ENGINE=softcut`) — a dual-deck crossfaded overdub looper on monome's [softcut-lib](https://github.com/monome/softcut-lib), hardware-verified.** Four voices (two per deck) that play **and** record the same loop at once, with click-free crossfades and interpolated overdub — the sound-on-sound tape/shuttle can't do. Record-defines-loop semantics (Alt+Play to record/close), per-voice SD load and save. ([`docs/engines/softcut.md`](docs/engines/softcut.md), [`docs/dev/softcut-impl.md`](docs/dev/softcut-impl.md))

### Changed

- **All own-display engines migrated onto the shared indicator toolkit (`src/engine/indicators.h`).** Following a full-tree audit ([`indicator-comparison.md` §7](docs/dev/indicator-comparison.md)), the 13 engines that hand-rolled their LED grammar (bard, csound, chuck, softcut, radio, glitch, pstretch, reso, mosc, delay, qdelay, reverb, edrums) plus the Faust engines now call the toolkit instead of duplicating it: the 9 hand-rolled Alt-held selectors → `ring::selector`/`ring::slots`, the ~8 copies of the route L/C/R block → `led::route_leds`, meters/markers → `ring::level`/`ring::playhead`, softcut's libm-`cos` breathe + transport ladder → `motion::breathe_standby`/`transport_view`, and the per-engine near-miss palette constants (`0x00c0ff`/`0x00aaff`≈`kCyan`, reso's drifted mode hues) unified onto `pal::`. Also lit previously-dark indicators — `led::route_leds` on edrums, a non-blank `meter=false` floor on the Faust engines. All engines build clean; net-new indicators needing per-engine data (`ring::value` pickup feedback, `led::clock`, `led::cycle` for the LFO engines) are deferred. **On-panel appearance not yet hardware-verified.** ([`docs/dev/indicator-comparison.md`](docs/dev/indicator-comparison.md), `src/engine/indicators.h`)

- **Synced platform-level changes from upstream Spotykach v1.2.0/v1.2.1.** Ported the fork-worthy upstream improvements into the sk-engines platform — external-clock dropout **recovery + downbeat re-alignment** in `Transport`, **USB MIDI** over the rear USB-C (host-built, hardware test pending), the **SD-slot selector off-by-one** fix, a generic **WAV cue-point** capability (`CapWavCues` + `wav_cues.h`, platform-parsed so any engine can use it), and a **Hann-table dedup** that trims per-TU copies out of the audio hot path. The granular cue/DSP rewrite is intentionally not chased — the granular engine is the frozen compatibility reference. ([`docs/dev/1.2.0-upstream-sync.md`](docs/dev/1.2.0-upstream-sync.md))

- **CMake build (`CMakeLists.txt` + `Makefile.cmake`) brought to full parity with the Makefile.** The opt-in CMake path (previously WIP and broken for every engine) now builds the same 20 engines as the canonical Makefile — including the QSPI-execute `mosc`/`csound`/`chuck` that were Make-only — and its `SRAM_EXEC` gap vs the Makefile is closed to ~0.65%. The Makefile stays canonical.

- **gigaverb (`ENGINE=gigaverb`): SIZE now slews `roomsize` instead of writing it raw.** Raw writes made the reverb tank's delay taps jump and crackle on every knob step; SIZE now glides to its target (~40 ms one-pole) so turning it is quiet. Only `roomsize` is slewed. (`src/engine/gigaverb/gigaverb_engine.h`, `src/engine/gen/gen_engine.h`)

- **Project licensing is now MIT with documented GPLv3 exceptions.** Two new engines add GPLv3 code — `qdelay` (`src/dsp/diffuser.h` + `src/engine/qdelay/`) and `glitch` (`src/engine/glitch/`) — so any binary of those is GPLv3 while every other engine stays MIT. Documented in a `## License` section in [`README.md`](README.md), with per-engine `LICENSE`/`NOTICE.md` and SPDX headers.

### Removed

- **Reverb: the parked Freeverb voice (`freeverb.dsp` + `faust_kernel_freeverb.h`) is deleted.** It was never wired into the engine (active voices are Dattorro plate / Zita hall / Greyhole) and wasn't liked. No build or behaviour change.

### Fixed

- **softcut: SD-slot load-on-release, fresh-take length, and cleanups (from a review pass).** Alt+PITCH slot select now **previews** and loads only on release (committed in `prepare()` once the deck is free), instead of firing a blocking `f_open` on every slot crossed — so scrubbing the selector no longer stutters the main loop or error-flashes when a load is mid-flight (the `radio` engine's settle pattern), and a slot change is ignored while a fresh take is recording. A fresh take's record rate is also frozen at 1× while defining, so its loop length is how long you played rather than the integrated PITCH rate (the closed loop snaps back to unity, matching the Play/overdub engage). Plus: `process()` zero-fills the output tail if the audio block ever exceeds `kMaxFrames` (silence, not garbage — no effect at today's 96-frame block, well under the 256 cap), and an unused 1 KB `kZero` global is removed. The remaining review items — a benign load-while-rolling buffer morph and a one-block loop-bound torn-pair — are accepted as the standard word-atomic scalar-race class. (`src/engine/softcut/softcut_engine.{h,cpp}`)

- **edrums: Alt+PITCH (drum-tone select) now shows a live ring selector.** Choosing a drum's tone with Alt+PITCH gave no persistent feedback — only a brief flash, and only when the value actually changed. edrums now tracks the Alt-held state (`set_aux_active`) and, while Alt is held, draws a `ring::selector` of all five tones (the current one bright, in the focused drum's colour). Hardware-gated like the rest of the indicator work. (`src/engine/edrums/edrums_engine.{h,cpp}`)

- **`make -C host test` restored to green (the harness had rotted to where no test built).** Three pre-existing issues, all host-only (the ARM firmware was unaffected throughout): (1) a **first-party latent-include sweep** — 65 headers across `dsp/`/`granular/`/`graincloud/`/`transport/`/`hw/`/`ui/`/`memory/` used bare `size_t`/`uint*_t`/`std::sin` via transitive includes only the ARM toolchain supplies; added the missing `<cstdint>`/`<cstddef>`/`<cmath>` (add-only, no vendored code touched); (2) a **`test_engine_params` link failure** (`undefined LEDRing::clear`, `GranularEngine` vtable) that was a compile-cascade — resolved once the sweep let `led.ring.cpp`/`granular_engine.cpp` compile again; (3) a **use-after-free in `test_pstretch.cpp`** — the `HostArena` was scoped to just the `init()` call, so it was freed before `process()` ran and the engine's ~1 MB (mmap-backed) input ring dangled → SIGSEGV; the arenas are now pooled to outlive the engines (mirroring the persistent SDRAM arena on device). All 22 suites build and pass.

- **Host test suite builds under Apple clang 21 (macOS Tahoe).** Clang 21's stricter front-end broke `make -C host test` at three host-only spots (a missing `<cstddef>`, a non-const VLA array bound, and a stale stmlib include path); all 22 host suites pass again. The ARM firmware build was unaffected.

- **csound: `undefined reference to 'posix_memalign'` at link after the beta17 bump.** csound beta17's `csoundCallocAligned` calls C11 `aligned_alloc`, whose nano-libc implementation needs `posix_memalign` (absent under `nosys`). The SDRAM-pool shim now also `--wrap`s `aligned_alloc` and serves it from the pool, so `make dist` links the `csound` engine again. ([`docs/dev/csound-impl.md`](docs/dev/csound-impl.md))

## [0.5.1]

### Added

- **Real ChucK `MidiIn` on the bare-metal build (prototype; build-verified, on-hardware wake test pending).** `libchuck.a` is now compiled with ChucK's `MidiIn`/`MidiOut` device classes enabled (`__DISABLE_MIDI__` dropped from all three ChucK define sites — `scripts/fetch_chuck.sh`, the firmware `Makefile`, and `pod/Makefile.chuck`, kept ABI-consistent), but with `midiio_rtmidi`'s RtMidi backend stripped: there is no OS MIDI API or callback thread on bare metal, and `rtmidi.cpp` (166 KB) stays out. A new `MidiInManager::inject()` feeds bytes straight into ChucK's per-VM MIDI buffer + event-wake path, so a `.ck` patch can use the desktop-portable `MidiIn min; min.recv(msg);` idiom (`examples/chuck/midi_in.ck`) instead of host-specific globals. The **full channel-voice MIDI stream** is forwarded (real velocity, NoteOff, CC, pitch-bend, aftertouch, program change) plus system realtime (clock/start/continue/stop): a new `IEngine::handle_midi_message(status, d1, d2)` virtual (no-op for engines that only use `handle_midi_note`) is driven from the UI MIDI poll (`core.ui.midi.cpp`) for every representable event; `ChuckEngine` overrides it to enqueue raw bytes on a lock-free ring (`chuck_midi_in.h`), and `ChuckEngine::process()` drains it and injects into device 0 on the run() thread, right before `ck->run()`, so a shred blocked on `min => now` is woken that block. `ChuckEngine::handle_midi_note` is kept only to map channel→deck for the panel gate-in LED. The vendored-ChucK edits live in `scripts/patches/midi_daisy.patch`, applied idempotently by `scripts/fetch_chuck.sh` (because `thirdparty/chuck` is gitignored and regenerated). The firmware (`make engine-chuck`) links with matching ABI. Ported from the `daisy-apps` sibling project; replication guide in [`docs/dev/chuck-midi-in-porting.md`](docs/dev/chuck-midi-in-porting.md), and the user-facing capabilities + remaining incompatibilities (no `MidiOut`, single virtual device / no enumeration, 3-byte messages only) are documented in [`docs/engines/chuck.md`](docs/engines/chuck.md). (SysEx / system-common aren't representable in a 3-byte `MidiMsg` and are not forwarded; on-hardware wake test still pending.)

- **mosc engine (`ENGINE=mosc`) — a dual macro-oscillator on the Mutable Instruments [Plaits](https://github.com/pichenettes/eurorack/tree/master/plaits) DSP, hardware-verified.** `make engine-mosc` gives each deck a complete `plaits::Voice` — the **full 24-engine** Plaits synthesis voice (virtual analog, waveshaping, FM, wavetable, grain, additive, chord, speech, swarm, noise, particle, string, modal, drums, plus the newer "engine2" models: VA-VCF, phase distortion, 6-op FM, wave terrain, string machine, chiptune) — so the two decks are two independent oscillators. Knobs map Plaits' whole panel: PITCH = note, **Alt+PITCH = engine select (24 models)**, SIZE = harmonics, POS = timbre, ENV = morph, MOD_AMT = LPG decay, MODFREQ = LPG colour, MIX = level. The per-deck **Mode** switch picks **Gate** (each trigger strikes the LPG/decay envelope — Play pad / gate-in / MIDI / Seq) vs **Drone** (LPG bypassed, engine runs open), and the global **Routing** switch combines the two voices three ways: **Stereo** (A→L, B→R), **DoubleMono** (mono sum), and **GenerativeStereo** (an out/aux spread that uses Plaits' otherwise-discarded auxiliary output for width). The three per-deck CV jacks are wired: V/Oct → note, CV_SIZE_POS → harmonics, CV_MIX → timbre. Like csound/chuck it is a **`BOOT_QSPI`** build (the 24-engine voice is ~292 KB of code, too big for the 186 KB SRAM_EXEC, so it executes from QSPI flash) — but **unlike** them it still synthesises from the platform SDRAM arena (a normal arena engine that merely *executes* from QSPI), so no SD card or external library is needed; the Plaits DSP is vendored in-tree (`src/engine/mosc/thirdparty/plaits`). Cloned from the [reso](docs/engines/reso.md) engine (same author, same `stmlib`, same PIMPL + arena-placement pattern). User doc: [`docs/engines/mosc.md`](docs/engines/mosc.md); internals, the QSPI build, and the 24-engine order in [`docs/dev/mosc-impl.md`](docs/dev/mosc-impl.md).

- **Shared `stmlib` for the Mutable Instruments engines (reso + mosc).** Both engines depend on Émilie Gillet's `stmlib` support library; the two vendored copies were byte-for-byte identical (same upstream, unchanged for years), so they are now a single trimmed copy at `src/engine/common/thirdparty/stmlib` (the union of the Rings and Plaits closures, 19 files), shared via the `STMLIB_TP`/`STMLIB_INC` make vars. reso's former `src/engine/reso/thirdparty/stmlib` was removed (its tree is now `rings` only); this is a dedup with no functional change.

## [0.5.0]

### Added

- **ChucK engine (`ENGINE=chuck`) brought up and running on hardware.** `make engine-chuck` runs a full [ChucK](https://chuck.stanford.edu) VM behind `IEngine`: the engine creates a `ChucK` instance, compiles a built-in `.ck` program (a `SawOsc -> LPF -> dac` drone reading `speedA`/`mixA`/`sizeA` globals), runs the synthesis VM in the audio callback, and applies live panel input through ChucK's global-variable queue (`setGlobalFloat`). Like Csound it is a **`BOOT_QSPI`** build (libchuck's ~1.1 MB of code can't fit SRAM, so it executes from QSPI flash) with ChucK's STL/heap-heavy allocations routed to a free-capable SDRAM pool (the platform heap stays in SRAM). The static library is the WebChucK source subset + feature defines (no MIDI/network/threads/dlopen), cross-built from upstream `ccrma/chuck` (pinned `chuck-1.5.5.8`) by `scripts/fetch_chuck.sh` with a small shim sysroot standing in for the POSIX/libsndfile userland ChucK assumes. **Verified end-to-end on a bare Daisy Pod** (audible drone + working knobs; confirmed over SWD running in `Chuck_UGen::system_tick` / `Chuck_VM_Shreduler::advance` / `Chuck_Globals_Manager::handle_global_queue_messages`). The full bring-up - four independent root causes found by attaching a debugger, and the SWD flash/inspect tooling - is in [`docs/dev/chuck-pod-poc.md`](docs/dev/chuck-pod-poc.md); roadmap in [`docs/dev/chuck-impl.md`](docs/dev/chuck-impl.md).

- **ChucK SD `.ck` patch bank + Alt+PITCH live switching, hardware-verified on the spotykach.** A card of `chuck/0.ck`..`7.ck` programs turns the engine into a runtime-loadable synth: at boot it auto-loads the lowest-numbered slot (the built-in drone if the card has none); **hold Alt and turn PITCH** to scroll a per-patch ring selector and **release** to switch live; anything missing/empty/uncompilable falls back to the built-in so the unit always sounds. The live swap runs on **one persistent ChucK VM that is never torn down**: a swap stops the current patch's shreds (`removeAllShreds()` + a one-frame `run()` flush, so the old UGens detach from `dac` before the next patch sporks), then either **re-sporks the target patch's cached compiled `Chuck_VM_Code`** or, on the patch's first visit, compiles it once and pins the bytecode. This is the fix for a cumulative patch-swap **memory leak**: ChucK 1.5.5.8 never frees its built-in type system on VM teardown (`Chuck_Env::cleanup()` frees only the base types behind a `// TODO`; `~Chuck_Context` keeps its namespace on success), so *every* tear-down-and-reload strategy leaked — recompile-per-swap, `CK_MSG_CLEARVM` reset, and full VM destroy+recreate (the worst: it re-leaked the entire multi-MB type system every swap until the SDRAM pool exhausted and previously-fine patches began overrunning). With one VM + a compile-once cache, total compiles are bounded by the number of *distinct* patches, so memory rises once per patch then stays flat across unlimited cycling — confirmed on the cased unit by ear and by a new on-panel **SDRAM-pool meter** (`METER=1` build, ring B = live pool usage; ring A = CPU load). A always-on **CPU-overrun safeguard** mutes a patch whose `run()` can't keep up (solid-red rings) so the controls stay alive and you can Alt+PITCH away. The patch bank scan/quantizer is host-tested (`make -C host test-chuck-patch`). User doc: [`docs/engines/chuck.md`](docs/engines/chuck.md); the full root-cause + fix writeup and the roadmap are in [`docs/dev/chuck-impl.md`](docs/dev/chuck-impl.md). (Sound-file UGens — `SndBuf`/`WvIn`/`WvOut` — remain unavailable; a planned WAV-over-FatFs bridge for `SndBuf` is scoped as M6 in the dev doc.)

- **More `csound` example orchestras (`examples/csound/3..6.csd`).** Four new ready-to-copy patches exercising a broader slice of the core opcode set than the original drone / super-saw / poly-lead trio: **`3.csd`** a resonant "acid" voice (a self-oscillating `moogladder` ladder filter with an `lfo`-swept cutoff), **`4.csd`** a stereo reverb pad (drone + MIDI through a shared bus into `reverbsc`, with a `tanh`-limited dry/wet output), **`5.csd`** a dub echo (a feedback delay line via `delayr`/`delayw` + a movable `deltapi` tap), and **`6.csd`** a self-playing generative line (`randomh` sample-and-hold picking semitones on each `metro` tick). All follow the established conventions (the `speedA`/`sizeA`/… control channels, `instr 1` + `instr MidiNote`, the global-bus + limiter pattern for the effects) and use only core, table-less opcodes; all seven (0–6) pass desktop `csound --syntax-check-only`. (`examples/csound/README.md`)

### Changed

- **The `dfilter` engine has been renamed to `filter`.** Breaking build invocations and artifact names so update any scripts or muscle memory:

  | was | now |
  |---|---|
  | `make ENGINE=dfilter` | `make ENGINE=filter` |
  | `make engine-dfilter` | `make engine-filter` |
  | `make -C host test-dfilter` | `make -C host test-filter` |
  | `sk-dfilter-<version>.bin` | `sk-filter-<version>.bin` |

  **The engine is functionally identical** — it is still the generated-Faust **parallel (DoubleMono) dual-deck** demo (one independent resonant low-pass per channel); only the short name changed. Everything that referenced it was updated: the `src/engine/filter/` tree (`FilterEngine` / `SPK_ENGINE_FILTER` / the `fx_filter` Faust namespace), the `Makefile` / `CMakeLists.txt` / `engine_select.h` build blocks, the host test, `scripts/build_release.py`, the control diagram, and the docs ([`docs/engines/filter.md`](docs/engines/filter.md)). Previously released `sk-dfilter-*.bin` binaries (under `dist/`) keep their original names — they record what actually shipped.

### Fixed

- **ChucK no longer aborts/HardFaults at boot - four root causes (the engine was previously linking but dying on hardware with a dark LED).** None were ChucK-on-Cortex-M incompatibilities; the platform, bootloader, linker script, and SDRAM were always fine. (1) **`abort()` in `compileCode()`** - `--specs=nano.specs` ships libstdc++ **without exceptions**, so every `std::__throw_*` is a bare `bl abort`; ChucK stringifies float literals via `std::to_string` -> `vsnprintf("%f")`, which without float-printf returns a negative length -> `std::string(buf, buf-1)` -> `__throw_length_error` -> `abort`. Fixed by linking **`-u _printf_float`** (added to the `ENGINE=chuck` branch; was already in the csound branch). (2) **HardFault right after init** - the VM was **never `start()`ed**, so `ChucK::globals()` returned `NULL` until `run()` lazily auto-started the VM *inside the audio ISR*, racing the main loop. Fixed by calling **`_ck->start()`** in `init()` (single-threaded, before audio) and null-guarding `globals()`. (3) **Imprecise BusFault in the SDRAM allocator** - the pool (`CsoundPool`, shared by both QSPI engines) is **not reentrant**, and `setGlobalFloat`'s `new` on the main loop raced `ck->run()`'s allocations in the audio ISR, corrupting the free list. Fixed with a **PRIMASK critical section** around every pool op (`chuck_alloc.cpp`). (4) **Dead/noisy knobs** - the bare Pod harness called `set_param` in an unthrottled loop, flooding ChucK's 16384-slot global queue until the VM choked draining it (~100% CPU, main loop starved); reading every block then exposed ADC jitter as zipper noise. Fixed (Pod harness only) by reading knobs **once per audio block in the callback** with a **deadband + one-pole smooth**; the spotykach UI is already event-driven (`PotMoved` -> `_apply`), so it needs no equivalent. Debugging used new in-firmware crash capture (overrides of `abort`/`__assert_func` plus `g_chuck_init_stage`/`g_chuck_init_error` buffers, readable over SWD) and a `pod/daisy_qspi.cfg` OpenOCD config that flashes (`stmqspi` IS25LP064A) and attaches over ST-Link without DFU. All four fixes are in the spotykach `ENGINE=chuck` firmware, now **hardware-verified on the cased unit** (it boots, runs the VM, and makes sound). (`docs/dev/chuck-pod-poc.md`)

## [0.4.0]

### Added

- **Csound 7 engine (`ENGINE=csound`), verified on hardware.** `make engine-csound` runs a full [Csound](https://csound.com) 7 instance behind `IEngine`: an orchestra (`.csd`) is compiled and performed at runtime, so the **patch defines the sound** - swap the `.csd`, swap the synth, no C++ change. Csound's ~2 MB of code can't fit SRAM, so it is a **`BOOT_QSPI`** build that executes from QSPI flash, with Csound's heap in a free-capable SDRAM pool (the platform heap stays in SRAM). It loads an **SD patch bank** (`/csound/0.csd`..`7.csd`, plus a built-in fallback so a card-less unit always sounds) with **Alt+PITCH live switching** (a lock-free handoff recompiles the patch on the main loop without blocking audio), maps the panel knobs to Csound control channels (`chnget`), and plays the patch's `MidiNote` instrument from **MIDI NoteOn**. The static library is the **core opcode set only** (no plugin opcodes or soundfile I/O), fetched + cross-built from upstream `csound/csound` (pinned tag `7.0.0-beta.16`) by `scripts/fetch_csound.sh`. Host-tested (allocator, patch selector, MIDI ring, reload handoff); the full design, bring-up notes, and the one open item (a QSPI-XIP CPU perf pass) are in [`docs/engines/csound.md`](docs/engines/csound.md) + [`docs/dev/csound-impl.md`](docs/dev/csound-impl.md).

- **Grain cloud engine (`ENGINE=graincloud`).** A polyphonic grain **cloud** - dozens of grains with independent pitch / pan / position scattered over the recorded buffer, in contrast to granular's tape-loop scanning. Built as a self-contained **variant of the `granular` engine** (`SPK_GRAIN_GF`): `src/engine/graincloud/` is a copy of the granular tree, so it inherits recording, SD save/load, dual-deck, crossfade, FX and UI verbatim, with only the grain core swapped - `Generator::process` sums a [GrainFlowLib](https://github.com/composingcap/GrainflowLib) cloud (`gf_cloud.{h,cpp}`) reading granular's recorded buffer instead of driving the granular voice. Played exactly like granular; the cloud knobs map per deck (POS = centre, SIZE = grain size, PITCH = transpose, ENV = position spray, MODFREQ = density, MOD_AMT = pitch/pan spread, SOS = dry/wet, with direction / playhead-speed / vibrato / glisson on the Mode switch + Alt layers). The de-STL'd GrainflowLib is vendored under `src/engine/graincloud/thirdparty/`; builds at `-Os` (~97% SRAM_EXEC), 8 grains/deck (conservative - on-device scattered-read cost is unconfirmed; raise `kMaxGrains` after a `METER` run). Host-tested (`make -C host test-graincloud-kernel`). See [`docs/engines/graincloud.md`](docs/engines/graincloud.md) + [`docs/dev/graincloud-impl.md`](docs/dev/graincloud-impl.md).

## [0.3.2]

### Added

- **Generated Faust engines can now use both decks (`dfilter`, `voice`).** The Faust generator (`scripts/gen_faust_engine.py`) previously emitted only single-deck engines - `FaustEngine::set_param` discarded the deck index, so deck A and deck B knobs collapsed onto one control set (chorus). A manifest `deck_mode` key now selects two ways to use **both** decks as independent banks. **`parallel`** (DoubleMono): two instances of one **mono** kernel, deck A on the left channel and deck B on the right, each with its own knobs, on `FaustEngine<Traits>` with a new `decks` dimension (`decks==1` keeps the single-deck path byte-for-byte). **`series`** (chain): **two different** kernels wired A->B with deck A driving stage 1 and deck B stage 2 - covering FX->FX and instrument->FX - on a sibling `FaustChainEngine<Traits>` template (`src/engine/faust/faust_chain.h`); the stages get engine-scoped namespaces (`fx_<engine>_<stage>`) so stage names can't collide. Both advertise `CapDualDeck` and need no platform change (the platform already delivers every knob for both decks; `set_param` just honours the deck arg). Shipped with two demos: **`dfilter`** (parallel - a resonant low-pass per channel, ~83% SRAM_EXEC) and **`voice`** (series - a drone oscillator into a resonant filter, the instrument->FX case that also exercises the 0-input chain path, ~84%). Both filters share a control layout: **PITCH = cutoff, POS = reso, SIZE = drive, SOS = mix** (on `voice` this is deck B; deck A is the oscillator's Pitch=freq / Size=shape / SOS=level). Each is `.dsp`(s) + a JSON manifest with no hand-written C++; the generator emits the wrapper, wires the build (`Makefile`/`engine_select.h`/`CMakeLists.txt` marker blocks), registers the kernel(s), and emits the control-diagram spec (deck B drawn as a `deckB` override for the series case). Host-tested for the property a single-deck engine structurally cannot have: per-deck/per-stage independence (`make -C host test-dfilter test-voice`). Still hand-written (out of scope): a runtime route/mode switch, per-deck voice allocation, >2 decks. Also hardens the Faust kernel recipe (`make faust-kernels`) to force UTF-8 (`PYTHONUTF8=1`) so cyfaust can emit kernels whose library metadata contains non-ASCII characters (e.g. `oscillators.lib`). (`docs/engine-types/faust.md`, `docs/engines/{dfilter,voice}.md`)

- **Dual virtual RadioMusic (`radio` engine).** `make ENGINE=radio` builds a streaming sample player modelled on Music Thing Modular's [RadioMusic](https://github.com/TomWhitwell/RadioMusic), as a **dual** instrument: each deck (A/B) is an independent virtual radio browsing one shared SD library of numbered banks (`/radio/0`..`/radio/15`), blended by the crossfader and placed by the routing switch. Its signature is the **free-running virtual playhead** - a per-deck monotonic frame clock advances every block, and tuning to a station seeks its file to `(clock + START) mod length` before streaming forward, so every station sounds as though it kept broadcasting while you were tuned elsewhere (leave one and return and it has advanced). Per deck: **PITCH** = station tuning (+ V/oct CV), **POS** = start offset (+ CV), **SIZE** = 0.5–2x varispeed, **ENV** = inter-station static (a filtered-noise "tuning hiss" crossfaded in on a switch), **MIX** = volume, **Alt+PITCH** = bank (held ring-dot selector), the mix fader blends A/B, the routing switch sets the stereo topology, and the **Play pad / gate-in** RESET a stopped deck. It reuses the `tape` engine's `SPK_USE_STREAM` streaming stack (lock-free per-deck SDRAM ring + main-loop FatFs pump) and adds a headerless raw-16-bit codec (`RawStreamReader`), a directory scan, and the playhead - so stations stream off SD with no in-RAM length cap (the original recordings run ~25 min / 130 MB; only a tiny per-station name+length index sits in RAM). Stations are **signed 16-bit mono PCM** at 48 kHz: a headerless `.raw` (the original RadioMusic format, length = filesize/2) assumes 48 kHz unless an optional one-line `/radio/rate.txt` rebases the resampler, so an unconverted 44.1 kHz card plays at correct pitch. `scripts/convert_radio_audio.py` converts or whole-card-`mirror`s a stock RadioMusic card (resampling 44.1→48 kHz, preserving names). `capabilities() = CapOwnDisplay | CapDualDeck | CapAux`. Host-tested (`make -C host test-radio`: the playhead modulo, station/bank quantization, varispeed, static, the codec) and verified on hardware; ~83% SRAM_EXEC. (`Makefile.cmake`, `docs/engines/radio.md`, `docs/dev/radio-impl.md`)

- **`radio` SD robustness: macOS metadata filter + anti-stutter guards.** Reading an original RadioMusic card copied via Finder, the bank scan indexed the hidden AppleDouble companions (`._NAME.raw`, ~4 KB of metadata macOS writes next to every `NAME.raw` on a FAT card) as bogus tiny "stations" that looped a sliver of garbage - a fast stutter, interleaved one-for-one with the real stations as you tuned. `scan_bank` now drops them three ways: the FAT hidden/system attribute (`AM_HID|AM_SYS`, which macOS sets on dot-prefixed files), a leading-dot name, and a 32 KB minimum size; the converter skips dotfiles on the source side too. Three further guards stop any re-open-driven stutter, since a (re)open flushes and re-seeds the per-deck ring: **station-select hysteresis** (a deadband so CV/pot noise near a station boundary can't chatter the target), **settle-every-prepare** (a target oscillating on a boundary never settles, so the engine holds the last clean station instead of re-opening ~5x/s there), and a **debounced RESET that is a no-op on a live deck** (a repeated/floating-gate trigger can't flush a playing stream). Verified on hardware (the stutter was field-reported and is resolved). (`src/hw/stream_deck.cpp`, `src/engine/radio/radio_engine.{h,cpp}`, `scripts/convert_radio_audio.py`)

- **`radio`: 16-bit `.wav` stations with per-file sample rate.** Banks can now mix headerless `.raw` and **16-bit-mono PCM `.wav`** stations. A WAV body is int16 like a `.raw`, so they share one streaming path: `RawStreamReader::begin_wav` parses the header (a chunk walk validating `fmt=1`/16-bit/mono) for the body offset+length and the file's **own sample rate**, so a 44.1 kHz `.wav` plays at correct pitch with **no `rate.txt`** (the rate ratio is per-deck - the WAV header rate, or the global `rate.txt` for raw). Indexing a `.wav` costs one `f_open`+header-parse per file at bank load (a `.raw` stays a pure `f_stat`); playback cost is identical. The converter gains `--format wav`. Host-tested (header parse / seek / rewind / rate + rejection of stereo/8-bit/float, and the engine dispatching to the WAV path with the header rate rebasing playback).

- **`make diagrams`: render the d2 diagram sources to SVG.** The d2 sources moved to `docs/diagrams/` (sources) with their rendered SVGs in `docs/media/` (output), and a new `make diagrams` target renders each `docs/diagrams/*.d2` → `docs/media/<name>.svg` via the `d2` CLI - an incremental pattern rule (a diagram re-renders only when its source changes), with a clear error if `d2` isn't installed.

### Changed

- **gen~ backend unified onto the JSON-manifest method (no more hand-edited C++ knob map).** A gen~ engine's control map used to be a hand-tuned `index_of()` switch in the generated `<name>_engine.h`; it is now **generated from a hand-authored `<name>.json` manifest** - the same declarative method as the Faust generator. The manifest's `knobs` block maps a platform control name to a gen~ **param name** (e.g. `"Pitch": "bandwidth"`); `gen_engine.py` runs gen-dsp, reads the export's auto-`manifest.json` for the param index + range, resolves + **validates** each binding (a typo errors at generate time, not runtime), and emits `index_of()`. The control-name->`ParamId` resolver moved to `engine_gen_common.knob_to_paramid` (shared with Faust) and gained raw-`ParamId`-name support, so modifier-layer params with no friendly knob label (`Feedback`, `EnvSize`, ...) bind by their `ParamId` name. New `make gen-engine MANIFEST=<name>.json` (`NOGEN=1` = regenerate glue only, no gen-dsp; `FORCE=1` = overwrite); the positional `gen_engine.py <export> <name>` form stays as a one-time bootstrap that emits a positional default. **`gigaverb` is the worked example** (`src/engine/gigaverb/gigaverb.json`): regenerating from the manifest reproduces its exact eight-param map (Speed->bandwidth, Env->damping, Mix->dry, EnvSize->early, Pos->revtime, Size->roomsize, Feedback->spread, ModAmp->tail) and rebuilds behaviour-identical (~83.4% SRAM_EXEC). Both backends are now declarative JSON: Faust maps knobs->slider labels, gen~ maps knobs->param names. (`docs/engines/gigaverb.md`)

- **Reverb: legible display + a third all-Faust voice (Greyhole), gen~ gigaverb removed.** The reverb was visually dead - the rings lit only with the live output level, so the panel went dark whenever the input was quiet and showed nothing about the patch. `render()` now draws each ring as three layers at once: the **active algorithm** as the ring colour (plate blue / hall violet / greyhole teal) over a dim full-ring baseline (never fully dark, readable in silence); the **DECAY knob (PITCH)** as the bright arc length (turning it gives instant feedback); and the **output level** as the arc's brightness on top, so the ring pulses with signal and visibly fades out as the tail decays. The three mode L/C/R LEDs - which sit at the Reel/Slice/Drift switch - now show the **selected algorithm** in its colour (so the 3rd position reads as a distinct teal) instead of the route (route stays legible from the two independent per-deck rings in DoubleMono). The **third voice is now Greyhole** (Faust's `re.greyhole` - a modulated diffusion network with a feedback pitch-shifter, a lush evolving/ambient reverb) instead of the gen~ gigaverb, which sounded poor and pulled the gen~ runtime: the 3-position switch maps cleanly to plate/hall/greyhole, all-Faust, with **no gen~ dependency**. `greyhole.dsp` owns its dry/wet crossfade and maps all six knob roles (Feedback/Size/Damp/Diffusion/ModDepth/Mix) to a real control. Greyhole is the heaviest voice, so the three-voice build overflows `SRAM_EXEC` at `-O2` (~106%) and the reverb branch now builds at **`OPT = -Os`** (as reso does), fitting at **185.5 KB / 97.4%** with ~5 KB headroom; **its per-block device CPU is unmeasured** (verify on hardware). The standalone `ENGINE=gigaverb` engine is unaffected (only removed from the reverb); the obsolete `test_reverb_giga` is dropped, and `test_reverb` now exercises all three voices (its response sums both channels so a stereo-only effect like Greyhole's modulation registers). A lighter Freeverb alternative is kept in `freeverb.dsp` for an easy swap-back. ( `docs/engines/reverb.md`, `docs/dev/reverb-impl.md`)

- **Renamed the engine-generator make targets for backend symmetry.** `make engine-gen` (the Faust new-engine generator) is now **`make faust-engine`**, and the Faust kernel-compile target `make faust-gen` is now **`make faust-kernels`** (it compiles `.dsp` -> `faust_kernel_*.h`; it never produced *engines*, and one engine can have several kernels - so the old name misled). A single-engine **`make gen-engine`** was added as the gen~ analogue of `faust-engine` (wraps `scripts/gen_engine.py`; the per-spec batch form stays `gen-engines`) - its manifest-driven form is the "gen~ unified onto the JSON manifest" entry above. Both old names (`engine-gen`, `faust-gen`) are kept as deprecated aliases so nothing breaks. The layering now reads cleanly: `faust-engine` / `gen-engine` generate one engine from its source; `faust-kernels` compiles all Faust kernels; `gen-engines` regenerates all gen~ engines. Generated `<name>_engine.h` + kernel header comments, both generator scripts, and the docs were updated.

- **Shared Faust zone-capture: `reverb` and `tape` refactored onto `faust_capture.h`.** The Faust "bind a kernel's sliders to platform knobs" machinery (`CaptureUI : UI` + the `Bind`/`Role` structs) existed in **three** copies - a full one in `reverb_engine.cpp`, a trimmed one in `tape/tapefx.h`, and the shared `src/engine/faust/faust_capture.h` written for the generated-Faust path (the `chorus` demo). Both hand-written engines now use the shared header and their private copies are deleted, so one zone-capture implementation serves the generator, `reverb`, and `tape` (and `shuttle`, via `tapefx.h`). `reverb` keeps its `Knob` enum, which converts to the shared `Bind`'s generic `int` role index; the shared `CaptureUI` carries the optional "Level" output-gain pin reverb relies on. The slider-**default** capture the generator needs (to seed its boot param cache) is gated behind a `CaptureUI<bool CaptureDef>` template flag, so the hand-written engines instantiate `CaptureUI<false>` and pay nothing for it - reverb's `SRAM_EXEC` rises just **+288 B** (175408 -> 175696 B, 92.3%). Behaviour-identical and re-verified host-green (`reverb`/`tape FX`/`shuttle` suites), clean target builds of `reverb`/`tape`/`shuttle`/`gigaverb`/`chorus`, and **confirmed on hardware** (`reverb` both algorithms + `tape` flashed and verified working). `tape`'s six FX sliders are all native `[0,1]`, so the shared `Role::set` linear-map (`0 + v*1 = v`) writes the normalized value straight through exactly as the old `Cap` did. (`docs/dev/engine-gen.md`, `docs/engine-types/faust.md`)

- **Engine docs split into user-facing + developer-facing.** Each engine's documentation now separates audiences: `docs/engines/<name>.md` keeps the **user-facing** reference (what the engine is + signature behavior, the control map, routing/display, SD-card prep + file formats, the build/flash commands) and `docs/dev/<name>-impl.md` holds the **developer-facing** material (architecture and internal data flow, the file-by-file map, implementation constants and risks/watch-items, bring-up status, bug writeups, and the roadmap / "what's left"). Each engine doc links to its impl notes near the top. Applied across all engines, roughly halving the engine docs (e.g. tape 254→113, reso 169→65, edrums 245→141, radio 220→131); the existing `tape-impl.md`/`shuttle-impl.md` absorbed the moved content (deduped). `passthrough` (a minimal reference example) is left whole. The convention is recorded in `docs/engines/README.md`, which also gains the missing `radio` row. (`docs/engines/*.md`, `docs/dev/*-impl.md`)

## [0.3.0]

### Added

- **Edrums per-drum stop/start and mute.** The **plain Play pad** now stops/starts the **focused drum**'s sequencer (`on_play_pad`, `reverse=false`, previously a no-op): stopped, that drum makes no sound (no new triggers; tails ring out) but its **clock keeps running silently** - the pattern stays phase-locked to the grid (the ring playhead keeps sweeping), so restarting drops it back in **in sync** with the other drums/bar rather than drifted. **Alt + Play** (`on_record_pad`, unused by edrums) toggles the focused drum's **mute**: it keeps stepping but its audio is zeroed. Both address the focused drum like the knobs (Rev to reach the deck's other drum), so each of the four drums stops/mutes independently; a reset (`clear_sequence`) restores the deck's drums to running + unmuted. The Play LED tracks the focused drum - off when stopped, a dim hit-flash when running-but-muted, a full flash when running and sounding. Both are transport/performance state and **not persisted** (every drum boots running + unmuted), like the playback position. Host-tested (stopped never triggers / start resumes / muted-is-silent-while-running / unmute restores / stopping one drum leaves the deck's other drum playing). (`docs/engines/edrums.md`)

- **Edrums reset-to-defaults gesture (hold Alt+Seq).** Holding **Alt + Seq** on a deck for ~1.5 s (the platform's existing clear-sequence hold hook, previously unused by edrums) resets that deck's two drums to the factory kit - model, pitch, decay, the grit/flux macros (back to neutral), and the pattern (back to silent) - from the same `kBootKit` table `init()` seeds from, refocuses the deck to slot 0, and marks the kit dirty so the QSPI auto-save overwrites the stored preset (the reset persists). It is **per deck** (matching the per-deck gesture): reset kick/tom on deck A, snare/hat on deck B, or both for a full kit reset. A knob-pickup reseed is requested so the reset values take over cleanly - and `CoreUI::_reseed_focus` now also re-seeds the **grit/flux macro channels**, which additionally fixes a stale-pickup gap where a macro knob could jump after a Rev swap (edrums and shuttle). `init()` was refactored onto the `kBootKit` table so a reset reproduces the exact shipped kit. Host-tested (reset restores model/gain/drive/density/brightness, refocuses slot 0, leaves the other deck untouched, requests a reseed). (`docs/engines/edrums.md`)

- **Edrums kits auto-persist to QSPI flash (tweaks survive power-off).** A tweaked edrums kit now saves itself to flash and reloads at boot, with no save gesture. The full state of all four drums - model, pitch, gain, decay, the four grit/flux timbre macros, and the sequencer params (density/length/rotation/probability/division) - plus the route and per-deck focus is captured as a small POD blob (`EdrumsEngine::KitData`) and written via libDaisy `PersistentStorage`. **Save** is debounced (~1.5 s after the last change) and runs from the main loop, never the audio ISR, so a knob sweep coalesces into one flash write; **load** happens at `init()` before the platform seeds the knob pickups, so a recalled kit takes over with no knob jump (`MValue` pickup then holds each value until its pot moves), and the first-ever boot keeps the built-in defaults. Stored in a dedicated QSPI region (offset `0x10000`, slug `edk`) clear of the calibration `Settings` (offset 0) and the app image (offset `0x40000`); the `PersistentStorage` template version is pinned at **1 permanently** to avoid libDaisy's `bkpt`-on-version-mismatch, with layout changes versioned instead by `KitData::version` (`apply()` rejects a mismatch -> defaults). serialize/apply are pure and host-tested (a full round-trip plus a version-reject check); the QSPI read/write is target-only (`#ifndef TEST`) and **pending hardware verification**. Adds an opaque `EngineContext::qspi` handle (a `void*`, so the platform-agnostic contract header pulls in no HAL type). Not yet: multiple named presets/banks (SD-card files with a save/select gesture). (`docs/engines/edrums.md`)

- **Shuttle gains the tape FX (wow/flutter + Jiles-Atherton saturation + resonant low-pass).** The `shuttle` engine can now colour playback with the same FX chain as the `tape` engine, applied **per track** (4 kernel instances, focused-track addressing, repointed on a Rev swap). The kernel wrapper was factored out of `tape_engine.cpp` into a shared `src/engine/tape/tapefx.h` that both engines include (`tape` re-verified, byte-identical). Because `shuttle` already uses POS/SIZE for the loop window (where `tape` puts drive/char), the FX is driven from this engine's free controls: the **grit pad = saturation** (grit+PITCH=drive, grit+MIX=character), the **flux pad = filter** (flux+PITCH=cutoff, flux+MIX=resonance), and **MOD_AMT/MODFREQ = wow/flutter** depth/rate. **Neutral settings bypass the kernel entirely** (skipped, not run flat) - the wow/flutter delay line imposes a fixed ~25 ms delay even at zero depth and the filter is not bit-identity even fully open, so bypassing keeps shuttle's varispeed playback bit-faithful when the FX is untouched and confines the Jiles-Atherton cost to tracks both rolling and using the FX; the summed bus is `SoftLimit`ed only while the FX is engaged, staying a plain linear sum otherwise. Cutoff boots fully open (filter inert until swept down). Host-verified (`make -C host test-shuttle`, an 11th test group: param round-trip + boot defaults, saturation wired/bounded, low-cutoff attenuation, wow/flutter modulation, neutral-bypass-is-bit-faithful). Builds at ~89% SRAM_EXEC; the four-instance CPU load is pending a `METER=1` hardware check. (`docs/engines/shuttle.md`)

- **Prebuilt engine binaries: a `make dist` release pipeline.** For users who want to download-and-flash rather than build (no ARM toolchain / cyfaust+gen-dsp venv needed), `make dist` (`scripts/build_release.py`) does a clean per-engine build of a curated "mature" set - the default list lives in the script and is overridable with `RELEASE_ENGINES=...` or CLI args - and collects the results under `dist/<version>/` as `sk-<engine>-<version>.bin` plus a `SHA256SUMS`, a `MANIFEST.txt`, and a `FLASHING.md`. The release **version** defaults to `git describe --tags --always` (a bare tag like `0.3.0` on a clean tagged checkout) and is passed through to the build as `SPK_VERSION` so the in-binary banner matches the artifact name exactly - the script **asserts** every `.bin` contains the expected `spotykach <version> engine=<engine>` banner before trusting it. `FLASHING.md` documents two flash paths: the **Daisy Web Programmer** (`flash.daisy.audio`, WebUSB/Chrome - the easiest, no toolchain) and **`dfu-util`** (QSPI app address, mirrored from the core Makefile). The `.hex` is **optional**, off by default since both flash paths use `.bin` (`--hex`, or `make dist WITH_HEX=1` - the toggle is `WITH_HEX` because the core Makefile already defines `HEX`). `make gh-release VERSION=x` uploads a built `dist/<version>/` to a GitHub release. The script is stdlib-only (plain `python3`, no venv) and host-tested.

- **Firmware version identity baked into every binary.** Each build now embeds a banner string - `spotykach <version> engine=<engine>`, where `<version>` is `git describe --tags --always` of the source tree - so any binary is self-identifying three ways: via `arm-none-eabi-strings <file>.bin | grep '^spotykach '` (works on a release `.bin` with no serial/display), a boot log line over USB serial (log builds), and the `infrasonic::firmware_version()` / `engine_name()` accessors for future on-device display. The version reaches the compiler as `-DSPK_VERSION_STR` from a Makefile `SPK_VERSION` (omitting `--dirty` so it only changes per commit, not per edit); a `build/.version-stamp` (mirroring the existing `.engine-stamp`) forces just `version.o` + `app.o` to recompile when it changes. The banner literal is anchored against `-Wl,--gc-sections` (a `used` attribute plus a log-independent reference in boot code) so it survives in release builds where nothing logs it.

- **Delay engine gains characters, topologies, modulation, and freeze.** Beyond the tempo-synced clean delay, the `delay` engine now has: a **feedback tone** control on **ENV** (a one-pole low-pass in the loop - up = open/clean, down = darker dub repeats); three **characters** on the **Reel/Slice/Drift switch** (`ConfigId::Mode`, per deck) - **Clean** (bit-for-bit as before), **Tape** (a wow/flutter LFO on the read time + the tone low-pass + soft saturation), **Shimmer** (the feedback pitch-shifted +12 each pass into an octave wash); three **topologies** on the **route switch** (`ConfigId::Route`) - **DoubleMono** (two independent mono delays, as before), **Stereo** (linked, a coherent stereo delay), and **Ping-pong** (linked + cross-feedback, echoes bouncing L↔R); a **modulation LFO** on the previously-unused **MODFREQ** (rate, `set_mod_speed`) + **MOD_AMT** (depth, `ModAmp`) that modulates the delay time for chorus/flange/vibrato (Tape's wow is the same LFO with a floor); and **Freeze** on the **Play pad** (`on_play_pad`, per deck) - loops the buffer at unity feedback while the input still passes dry, so you can play over a held loop. The inline pitch shifter was refactored into a reusable `Shifter` (output transpose + shimmer feedback), and `Tap::process` split into `read_color()`/`write_out()` so ping-pong can cross the two taps' feedback; Tape/Shimmer soft-clip the loop for stability while Clean stays bit-for-bit. Host-tested (`make -C host test-delay`, 7 groups: ring-out + feedback length, distinct/finite characters, ping-pong cross-feed vs DoubleMono isolation, ENV tone, max-feedback stability, modulation, freeze-sustains-vs-decay). Builds at ~80% SRAM_EXEC. (`docs/engines/delay.md`)

- **Route-aware reverb: DoubleMono runs two mono plates (heavy voices stay single).** The `reverb` engine now reads the panel **routing switch** (`ConfigId::Route`) and adapts at runtime: **Stereo / GenerativeStereo** = one stereo voice (deck A's selection) over the stereo pair (deck B's strip inert); **DoubleMono** = an independent **mono PLATE per deck** - deck A reverberates the left input into the left output, deck B the right, each driven by its own knob bank, channel-isolated (a silent input stays silent, host-verified). A full voice set is allocated **per deck** up front (`_rv[DeckRef::Count][kReverbCount]`, ~2.1 MB Faust / ~6.2 MB with gigaverb, trivial against the 48 MB arena), so the route is a runtime branch in `process()` with no reallocation. `capabilities()` gains `CapDualDeck`; `route()` reports topology and the three mode L/C/R LEDs show the route.

  **The DoubleMono cap (plate-only).** The Zita hall and gen~ gigaverb are heavy and SDRAM-bound; running two at once thrashes the H7 D-cache. Measured on hardware via the METER build: one hall ~18.6%, **two halls ~70% avg / ~89% peak** (super-linear ~3.8x - memory-latency-bound, which the host bench couldn't model), two plates ~41% / ~61%. So DoubleMono is capped to **plate-only** (`eff_voice()` forces plate; the heavy voices are stereo-route-only and can never run two-up), keeping the worst case at two plates (~61% peak) with comfortable headroom. The mode-switch selection is remembered across route changes. (`docs/engines/reverb.md`)

- **Reverb algorithm selection moved to the Reel/Slice/Drift mode switch.** The plate/hall (/gigaverb) selector is now the dedicated **3-position mode switch** (`ConfigId::Mode`) - one tactile position per algorithm - replacing the old held **Alt+PITCH** (`CapAux`) knob gesture. The switch only takes effect in a stereo route (deck A selects the voice); in DoubleMono the voice is forced to plate (see the cap above). Switching is a pointer flip that re-applies the deck's cached knob values to the newly-active kernel (no stranded knob). `CapAux` and the held-selector ring render are gone. Also: **Decay moved to the PITCH knob** (the biggest knob, the most impactful character control), swapping with POS which now drives Tone.

- **Optional gen~ gigaverb as a third reverb voice.** `make ENGINE=reverb REVERB_GIGAVERB=1` folds Tom Erbe's gigaverb (the [gen~ export](docs/engines/gigaverb.md)) into the reverb engine as a third selectable voice. It is wrapped as a non-Faust `GigaverbVoice` (driving the gen~ export by parameter index), allocates its gen~ state from a dedicated ~2 MB arena slice **per deck** (two independent gen~ instances are safe - genlib data objects are per-allocation, no global name registry), and synthesizes a true wet/dry crossfade in software (pins gen~ `dry` to 0, blends against the input) so its Mix knob matches the Faust voices. Pulls the gen~ runtime in (~9 KB code, SRAM_EXEC ~97%); plain `make ENGINE=reverb` stays a lean two-voice build.

- **Reverb host test + cost benchmark + on-device CPU meter toggle.** `make -C host test-reverb` exercises the reverb through the public `IEngine` surface (param round-trip, Mode/Route change semantics, both algorithms ringing out, every knob role bound, the mode switch swapping the kernel, the DoubleMono plate-only cap, and channel isolation); `make -C host bench-reverb` times `process()` for both topologies. A global `make ... METER=1` opt-in enables the existing `Meter::cpu` `CpuLoadMeter` for any engine, writing `load% max/avg/min` to the external USB CDC every ~250 ms via a **direct non-blocking transmit** (drops if the host isn't draining) - so the meter can never hang the main loop the way the daisy Logger's blocking `TransmitSync` does.

- **Buffer-based bipolar/reverse varispeed tape engine (`shuttle`).** `make ENGINE=shuttle` builds the random-access counterpart to the streaming `tape` engine: audio lives in SDRAM (not SD-streamed), so the read pointer is random-access and **reverse**, **freeze**, and **loop windowing** are trivial - the trade is a finite, RAM-capped length (currently 30 s/track). It runs **four independent mono tape tracks** - two per deck (A/B) - which free-run at their own speeds and sum into the stereo bus. Each track is a contiguous `float32` buffer carved from the SDRAM arena (1.44 M frames / 5.76 MB at 30 s/48 kHz; 4 tracks = 23 MB of the 48 MB arena), played by a **signed fractional read pointer** (`double`); reverse is a negative speed, freeze is speed 0 -> silence, ends wrap. `CapOwnDisplay | CapDualDeck | CapAux | CapAltPos | CapPitchPickup`. Host-verified (10 test groups), builds + links on target (~82% `SRAM_EXEC`), pending hardware bring-up. (`docs/engines/shuttle.md`, `docs/dev/shuttle_impl.md`)

- **Shuttle PITCH = bipolar capstan speed; Play snaps to unity.** PITCH is a per-track bipolar speed knob: a deadzone about **noon = stop (silence)**, clockwise = forward to **+2x**, counter-clockwise = **reverse to -2x**. Unity (+1x) lands off-centre, so the **Play pad snaps** the focused track to +1x and arms a one-shot pickup re-seed so the absolute pot must be swept across unity before it retakes the speed.

- **Shuttle four-track focus (Rev swap) + global re-align (Seq pad).** Each deck holds two tracks; all four play at once, but a deck's knobs (PITCH/POS/SIZE/MIX/Alt+PITCH) and ring address only the **focused** track. Tapping a deck's **Rev pad** swaps the focus (edrums mechanism: the backgrounded track keeps playing; a one-shot re-seed catches the pots without jumps). The four tracks drift out of phase by design; the **Seq pad re-aligns all four** to their loop start in one atomic gesture, declicked per audibly-rolling track (a ~1.3 ms fade-out -> jump -> fade-in, applied in the ISR off a flag so it is race-free and all four snap on the same block).

- **Shuttle loop window (POS / SIZE).** Each track plays a window `[start, start+length)` of its buffer rather than the whole recording: **SIZE** sets the length (full buffer down to a `kMinLoopFrames` stutter floor), **POS** slides the start across the uncovered tail (the window always stays inside the recording; at SIZE = full, POS is inert). The varispeed pointer wraps within the window both directions, and the Seq re-align snaps to the window start. Both are per-track (repoint on a Rev swap).

- **Shuttle audio source: record + SD load.** A track fills its buffer by **recording** the live input (Alt+Play, overwrite from the start) or by **loading** an existing `/shuttle/` WAV from the card into RAM (Alt+PITCH selects the slot), drained over a few `prepare()` passes; the drain advances by `play_consume`'s actual returned byte count (zero-fill is destination-only), so an underrun is re-pulled, never baked in as silence. The shuttle keeps its files in its **own `/shuttle/` folder** (filenames `tape_<a|b>_<n>.wav`), isolated from the streaming `tape` engine's `/tapes/` so the two engines never share or overwrite each other's takes.

- **Shuttle boot preload (all four tracks).** On power-up the shuttle fills **all four tracks** from the card if their files exist - track *t* of each deck loads slot *t*'s file (deck A: `tape_a_1/_2.wav` -> tracks 0/1; deck B: `tape_b_1/_2.wav`) - so a freshly powered unit is ready to jam without a manual Alt+PITCH load (the RAM engine can't stream lazily the way `tape` does). Per-track conditional (an absent slot file leaves that track empty) and, since a deck's two tracks share one SD stream, **serialized per deck** (the two decks load in parallel). The card mounts cooperatively early in boot, so the load waits across `prepare()` passes until the volume is up, then is abandoned after a boot deadline if there is no card / no files.

- **Shuttle per-track pan + routing switch.** The four tracks now pan **individually** into the stereo bus - previously each deck summed its two tracks to mono before a single deck-level pan, so the two tracks of a deck could only share one pan position. The panel **routing switch** (`ConfigId::Route`, shown on the mode L/C/R LED) picks where each track's pan comes from: **LEFT** (DoubleMono) = the manual per-track Alt+POS pan; **CENTRE** (Stereo) = auto-spread, a deck's two tracks fanned hard L/R for instant stereo width; **RIGHT** (GenerativeStereo) = a random equal-power pan per track (LCG, no platform RNG), rolled **once on entry** - unlike the tape engine, shuttle guards the roll on the actual switch transition because the platform calls `set_config` every loop. Alt+POS became a per-track knob (was per-deck), so the platform now also re-seeds `ParamId::AltPos` on a Rev focus swap to keep the pot pickup from jumping.

- **Shuttle idle ring reads as "on", with a per-track hue.** A loaded-but-stopped deck previously painted its ring black (looked powered-off); it now breathes a dim standby glow (a slow raised-cosine pulse off the engine's own clock) in the **focused track's hue** - track 1 indigo, track 2 amber - so a glance tells you which of a deck's two tracks is in focus. Live transport keeps its direction colors (record red / forward green / reverse cyan / frozen dim-white), and the Rev-swap flash now uses the newly-focused track's hue so the color change itself signals the swap.

- **`CapPitchPickup`: route PITCH through the pickup-gated path.** A new capability so an engine that snaps the speed (shuttle's Play->unity) can hold the snap via `take_param_reseed` - the absolute PITCH pot must be swept across the snapped value before it retakes control. Engines without it keep raw (un-gated) PITCH byte-for-byte.

- **Headless shuttle test.** `make -C host test-shuttle` runs `host/test_shuttle.cpp` (10 groups, via a controllable `FakeClock` for the pad debounce and a per-deck `FakeStream` for the SD-load drain): bipolar speed map, record + unity playback, reverse-wrap + silence-at-noon + loop, Play->unity snap (one re-seed), Rev swap + track independence, SD load, Seq re-align (all four atomic), realign declick (no large output step across the jump), the POS/SIZE loop window, and the boot preload (fills all four tracks from their slot files, serialized per deck, skipping a track whose file is absent).

- **Audio-prep script + docs for the streaming format.** Both streaming engines load only mono 32-bit-float WAV at 48 kHz, so `scripts/convert_tape_audio.py` batch-converts arbitrary sources (via ffmpeg or sox), verifies each output header against the firmware's requirement, and can name files straight into deck slots (`--engine tape|shuttle`, `--deck a|b`, `--start-slot`); it warns past the shuttle's ~30 s RAM cap. `docs/preparing-audio.md` documents the format and the ffmpeg/sox one-liners, linked from the tape and shuttle engine docs.

- **Tape: post-FX resonant low-pass filter on the grit pad, soft-limited bus.** Each tape deck gains a 2-pole resonant low-pass after the wow/flutter + Jiles-Atherton chain - a dub/tape tone control. It rides the **grit knob-edit gesture** rather than dedicated knobs: hold **grit + PITCH** = cutoff (exp, ~40 Hz .. 20 kHz), hold **grit + MIX** = resonance (Q 0.7 .. ~10, squared so the low half of the knob stays gentle), reusing the platform's existing `GritIntensity`/`GritMix` params - the tape engine just reinterprets them, so there is **no CoreUI/platform change**. Cutoff boots **open** so the filter is inert until swept down (its pickup is seeded open -> turn down to engage). The summed two-deck output bus is then **soft-limited** (`daisysp::SoftLimit`, the cubic soft-clip the edrums/granular buses use) so the resonant peak and two simultaneous decks can't clip the codec. Host-verified (low cutoff attenuates a 6 kHz tone, resonance boosts near the corner, limiter caps the peak at ~0 dBFS); pending hardware bring-up.

- **CMake build at parity with the Makefile.** The opt-in CMake path now resolves `reverb`, `shuttle`, and `gigaverb` (the gen~ engine's full define/include/warning set), and the `Makefile.cmake` frontend gains the matching `engine-*` flash targets plus the host-side `faust-gen`/`gen-engines`/`test-scripts` targets. All three configure and compile; `gigaverb` linked end-to-end. The Make build stays canonical.

### Changed

- **Edrums voices are now tweakable live (grit/flux macros + per-drum gain).** The reworked synth voices were static because the sequencer owned every knob; sound was fixed per model. Now the **SOS knob is per-drum gain** (the previously-missing mix/level control), **decay moved to `grit+SOS`**, and the **grit/flux pad modifiers expose four per-drum timbre macros** - `grit+PITCH`=drive, `flux+PITCH`=pitch-sweep amount, `flux+SOS`=body/noise balance, `flux+POS`=noise brightness - each a **bipolar offset on the model baseline** (noon = the model unchanged, so a fresh kit sounds identical until tweaked) addressing the focused drum and repointing on a Rev swap. That gives eight live sound axes per drum (pitch, gain, decay, drive, sweep, brightness, body/noise, model), all four drums independent. It rides the platform's existing grit/flux held-modifier layer (`GritIntensity`/`GritMix`/`FluxIntensity`/`FluxMix`/`FluxFb`, all pickup-seeded), so **no platform change** - decay sits on `grit+SOS` rather than `Alt+SOS` precisely because the `Feedback` channel the latter routes to is not seeded. Host-checked (`make -C host test-edrums`: SOS=gain incl. silence at 0 and louder with more, decay-on-`grit+SOS` lengthens the tail, all timbre macros finite/bounded when pushed). Not yet exposed: an independent body-vs-noise snap decay (needs a sixth modifier channel) and saving tweaked kits as presets.

- **Edrums synthesized voices reworked for depth and realism.** The `edrums` `Voice` gained, per model: **independent body and noise amp envelopes** (`noise_decay`) so the snap and the tonal ring decay on their own timescales (the biggest realism limiter before - one shared envelope); a **per-model pitch-envelope time** (`pitch_ms`, replacing the fixed ~25 ms) so the kick gets a long ~55 ms drop and the hat ~5 ms; a **high-passed hat** (the noise filter is now a runtime-typed `BiquadSection` switched band-pass/high-pass, vs the old dull narrow band-pass) for a metallic "tss"; an optional **second detuned body partial** (`partial_ratio`/`partial_mix`) for snare/tom richness; gentle **body saturation** (`drive` -> soft-clip) and an **attack click** (`click`, kick beater / tom mallet); and a per-model **`base_scale`** so each drum tunes to its own range under the one shared PITCH knob (e.g. a ~60 Hz kick despite the platform's 0.5 default). All five voices are now defined by a single `kModels` table - the one surface to tune by ear. Host-checked (`make -C host test-edrums`, a per-model group: every model renders finite/bounded/audible, and the high-passed hat measures far brighter than the kick's sine body). Builds at ~84% SRAM_EXEC; the voicing constants are first-principles starting points pending an on-hardware listen.

- **Tape wow/flutter rate favors very low frequencies.** The MODFREQ -> rate map was linear with too high a floor (`0.5 .. 2.5 Hz` wow / `6 .. 12 Hz` flutter), so even the lowest setting felt too fast. It is now a **cubic** curve with a low floor (`0.1 .. 2.5 Hz` wow / `0.5 .. 12 Hz` flutter): the bottom of the knob is a slow drift (~10 s period) and the rate climbs to a reasonable wobble only near the top. Endpoints (top speed) are unchanged.

- **gen~ engine build targets drop the `gen_` prefix.** The gigaverb engine renames `gen_gigaverb` -> `gigaverb` (`ENGINE=gigaverb`, `src/engine/gigaverb/`, `SPK_ENGINE_GIGAVERB`), and the generator convention changes to match: `scripts/gen_engine.py` now wires a gen~ export as a plain `ENGINE=<name>` / `src/engine/<name>/` / `SPK_ENGINE_<NAME>` (no `gen_`/`SPK_ENGINE_GEN_`), so a gen~ engine sits beside native and Faust engines with no prefix - and the only `gen`-named directory is the shared infra `src/engine/gen/`. The gen~ provenance now lives in the engine's docs + `manifest.json`, not its target name.

- **SD streaming service gated by capability (`SPK_USE_STREAM`), not engine name.** The streaming platform (`StreamDeck`/`FatFile`, SDRAM rings, keep-card-mounted) was guarded by `#if defined(SPK_ENGINE_TAPE)`; it now keys on a Makefile-set `SPK_USE_STREAM` flag that both `tape` and `shuttle` define. The `app.cpp` construct/pump/inject of `_stream` is the one shared touch point (a hardware service the platform must own); the rest now reference the capability, so the next streaming engine is a one-line Makefile change. Non-streaming engines stay byte-identical (`tape` output unchanged - both macros are defined for a tape build).

### Fixed

- **Streaming WAV load: validate format + tolerate metadata chunks (was silent distortion / no-load).** The shared `WavStreamReader` (tape + shuttle playback/load) handed a file's raw body bytes straight into the engine's mono-float frames with no format check, so a 16-bit, 32-bit-**integer**, or stereo WAV was reinterpreted bit-for-bit as float32 - extreme full-scale distortion (a small int sample becomes a ~1e13 float). It also parsed only the first 64 header bytes, so an externally-authored file whose `data` chunk sits behind a `fact`/`LIST` metadata block (e.g. ffmpeg/DAW float output lands it at offset 90-128) silently failed to load. `begin()` now (a) **walks the RIFF chunk list by seeking** - skipping any `fact`/`LIST`/`JUNK` metadata of any size with no fixed offset ceiling, and handling `WAVE_FORMAT_EXTENSIBLE` - to locate `data`, exactly as a conformant reader must, and (b) rejects anything that is not the native record format - `AudioFormat`/`BitsPerSample` per the build (float32 default, int16 under `LOFI_INT16`), mono, 48 kHz - returning false so `start_play` raises the deck's error flash instead of playing garbage. The format checks are engine-capability gates (the stream path does no conversion), not WAV-spec strictness. Device-recorded tapes (canonical mono-float 44-byte header) are unaffected. New host tests cover the rejects (16-bit / int / stereo / non-48k), the native accept, and parsing `data` behind `fact`/`LIST` metadata at any offset (incl. the real ffmpeg/Logic layout, a chunk before `fmt `, and odd-sized chunks).

- **Tape FX boots OFF (was a colored, un-reducible default).** The per-deck wow/flutter + Jiles-Atherton FX seeded its four knobs at non-zero defaults (drive/char/wow 0.3, rate 0.4), so a freshly loaded tape played with audible wow/flutter and saturation. Worse, the platform seeds each knob's pickup from those values, so a pot physically below the seed could never be swept down across it (soft-takeover) - the wow/flutter literally could not be turned off (it read as a "record needle moving between grooves"). `_fx_n` now defaults to all-zero per deck: the FX boots neutral and every param is freely reducible from zero. (`src/engine/tape/tape_engine.h`)

- **Audio-prep script writes the canonical WAV header itself (was noise on playback).** `scripts/convert_tape_audio.py` let ffmpeg/sox author the WAV container, which for 32-bit float **always** carries a `fact` chunk (and a DAW source's `LIST`/INFO metadata rode along), pushing the `data` body to offset 58-128 instead of the canonical 44 - all spec-valid, but a firmware reader that keys on the canonical offset then played that metadata as audio (structured noise). The script now decodes to **raw float** (`ffmpeg -f f32le` / `sox -t f32`) and writes a byte-exact copy of the firmware's own 44-byte header (`data` at 44, no `fact`/`LIST`), so output plays on any reader regardless of how its header parser works. Pairs with the chunk-walking reader above: either fix alone resolves the noise.

- **Tape: a wrong-format file now flashes distinctly from an empty slot.** A rejected load **strobes** the deck's amber error indicator (~4.5 Hz) when the file exists but was refused for its format, versus the **steady** amber of a missing/empty slot - so "convert this file" is distinguishable from "nothing here" on the panel. The cause is detected via `IStreamDeck::exists` (present-but-refused = format).

- **CMake `tape` build was unlinkable (missing `SPK_USE_STREAM`).** The CMake `tape` branch defined only `SPK_ENGINE_TAPE`, but the SD-streaming TUs (`stream_deck`/`fat_file`/`buffer.sdram`) gate their bodies on `SPK_USE_STREAM`, so they compiled empty and the link failed on undefined `StreamDeck`/`FatFile` symbols. The branch now sets `SPK_USE_STREAM`, matching the Makefile.

## [0.2.4]

### Added

- **Faust-generated reverb engine (`reverb`).** `make ENGINE=reverb` builds a stereo reverb with two switchable algorithms - a Dattorro plate and a Zita-rev1 hall - selected live on Alt+PITCH. The DSP is generated from Faust sources by cyfaust (`make faust-gen`); the kernels are checked in, so the firmware build needs no cyfaust. (`src/engine/reverb/`, `docs/engines/reverb.md`)

- **Tape FX: wow/flutter + tape saturation.** Each tape deck gains an analog-tape effect on its playback signal - wow/flutter into Jiles-Atherton hysteresis/saturation, a per-deck Faust kernel. New per-deck knobs: POS = drive, SIZE = character, MOD_AMT = wow/flutter depth, MODFREQ = rate (POS was previously unused). (`src/engine/tape/`, `docs/engines/tape.md`)

- **gen~ engine support via gen-dsp, and the `gigaverb` engine.** `make ENGINE=gen_gigaverb` builds a stereo reverb authored in Max/MSP **gen~** (Tom Erbe's gigaverb) and translated to C++ by [gen-dsp](https://github.com/shakfu/gen-dsp) - the gen~ counterpart of the Faust path. A host-side generator (`scripts/gen_engine.py`, or `make gen-engines`) turns a gen~ export into a wired-in engine; the generated files are checked in, so the firmware build needs no gen-dsp. gigaverb is 2-in/2-out, 8 params, stereo + knobs only. (`src/engine/gen/`, `src/engine/gen_gigaverb/`, `scripts/gen_engine.py`, `docs/engines/gigaverb.md`)

### Changed

- **Engine docs reorganized into methods vs implementations.** `docs/engines/` now holds only the shipped engines; a new `docs/engine-types/` documents the three ways to build one - native C++, Faust/cyfaust, and gen~/gen-dsp.

- **Edrums now boots silent instead of playing a full kit.** All four drums now seed with zero Euclidean onsets, so the kit boots silent and the player builds the groove up by raising POS on each drum (model, pitch, decay, and length stay pre-seeded, so each drum is voiced the instant its first onset appears).

## [0.2.3]

### Added

- **Dual streaming tape-deck engine (`tape`).** `make engine-tape` builds two independent **mono** tape decks (A/B) that stream record/playback to/from the SD card, removing the in-SDRAM loop-length cap (~42 s float / ~84 s int16, `kSourceMaxSeconds`) - each deck plays/records an arbitrarily long file. A deck is play-XOR-record (no overdub), so the two run like a pair of record decks (play A while recording B, then play both and beat-match by ear). The engine is thin: in the audio ISR it only moves float frames between the platform's lock-free per-deck SDRAM rings and the buffers; the slow FatFs I/O runs in a dual-deck `StreamDeck` pump on the main loop, reached via a per-deck `IStreamDeck*` injected through `EngineContext`. Native mono float WAV (no conversion); one 1 MB ring per deck + a 32 KB shared scratch; all guarded by `#if defined(SPK_ENGINE_TAPE)` so other engines stay byte-identical. `CapOwnDisplay | CapDualDeck | CapAux | CapAltPos`; NOT `CapTapeStorage`. Dual-deck core hardware-verified; ~81.5% `SRAM_EXEC`. See `docs/engines/tape.md`, `docs/dev/tape-impl.md`.

- **Tape transport + per-deck controls.** Per deck: **Play** = play, **Alt+Play** = record (300 ms debounce; Rev pad reserved for reverse); **PITCH** = varispeed (`exp2((v-0.5)*2)`, 0.5x..2x, +/-1 octave); **MIX** = playback volume. Two MONO inputs (deck A records input A, deck B input B - never summed) mix to a stereo bus driving the headphone + the individual jacks. Display per deck: idle ring off / green = play / red = record / amber = rejected start, on the Play-pad LED + ring.

- **Tape routing + pan.** The **routing switch** (`Route`) picks the topology - LEFT = each deck panned by its **Alt+POS** knob, CENTRE = both centered (mono sum), RIGHT = random pan - shown on the mode L/C/R LED. The **mix fader** (`Crossfade`) blends deck A/B (DJ-style: centre = both full, ends = one only).

- **Tape loop modes (ENV knob).** Per deck, by quadrant: none / plain loop / faded loop (~50 ms layer de-click) / Frippertronics (decays ~0.6x per pass, then auto-stops). Looping lives in the stream (`PlayStream` rewinds the WAV at EOF); the fade/decay is engine-side via `loop_frames` + source-frame tracking, with a Frippertronics fade-out flagged from the ISR and stopped in `prepare()`. (`src/memory/{audio_stream,wav_stream}.h`)

- **Tape slots - non-destructive takes.** 8 slots per deck under `/tapes/` (`tape_a_<n>.wav` / `tape_b_<n>.wav`), selected by **Alt+PITCH** (the ring shows the 8-slot selector while Alt is held); the next Play/record targets the active slot. The selector distinguishes **recorded vs empty** slots (selected bright / recorded mid / empty dim), probed via a new `IStreamDeck::exists` (`f_stat`) from the engine's `prepare()` when the selector opens. `FatFile::open_write` creates `/tapes` on first record; single-digit slots keep the names 8.3-safe.

- **`ParamId::AltPos` + `CapAltPos`: a reusable Alt+POS knob layer.** Mirrors `CapAux`/`Aux` (Alt+PITCH): an engine that advertises `CapAltPos` receives the Alt+POS knob as `ParamId::AltPos` (the tape engine reads it as per-deck pan); engines without it keep POS -> `Pos` byte-for-byte, so granular/delay/edrums/passthrough are unchanged.

- **Headless streaming test.** `make -C host test-stream` builds and runs `host/test_stream.cpp`, an end-to-end exercise of the streaming core over an in-memory `IByteFile` (ISR -> SPSC ring -> streaming-WAV file -> ring -> ISR), asserting a byte-exact round-trip plus the underrun/overrun, EOF, finalize-header-patch, and **loop-rewind** (a looping `PlayStream` repeats the WAV body seamlessly and never finishes) behaviour off-target. The core (`spsc_ring`, `audio_stream`, `wav_stream`, `byte_file`) is engine-agnostic and proven on the host before any device run.

### Fixed

- **Tape build keeps the SD card mounted for the session.** `Storage::process()` unmounts the card via `_can_unmount()` once settings are read and both decks are idle (`f_mount(NULL)` + `_fsi.DeInit()`), which tore the FatFs volume out from under the tape stream - every `f_open` then returned `FR_NOT_READY`, so record/play silently no-op'd and no tape file was ever created. Under `#if defined(SPK_ENGINE_TAPE)` the unmount is skipped so the volume stays mounted (the tape engine owns all SD I/O and `Storage` stays idle in that build); non-tape builds are unchanged. Found during hardware bring-up; see `docs/dev/tape-impl.md`.

## [0.2.2]

### Added

- **Resonator / pluck engine (`reso`).** `make engine-reso` builds a dual resonator/pluck voice on the Mutable Instruments **Rings** modal/string DSP (one `rings::Part` per deck, mono, polyphony 1) rather than a hand-rolled Karplus-Strong. A per-deck **mode switch** sets how the resonator is excited - **Slice** (discrete internal plucks), **Reel** (fed from the live input plus a short ~4 ms trigger burst), **Drift** (a free-running scatter cloud auto-strumming at randomized intervals) - and **Alt+PITCH** (`ParamId::Aux`) selects the Rings model (modal / sympathetic-string / string / FM / string+reverb). Controls: PITCH = note (5-octave C1..C6), SIZE = damping/decay, POS = excitation position, ENV = brightness, MOD_AMT = structure, SOS = dry/wet, MODFREQ = Drift density / Slice arp rate. The V/Oct CV input transposes the note *additively* on top of the knob (0 V = knob only). Advertises `CapOwnDisplay | CapDualDeck | CapAux | CapTransport`; deck A -> left out, deck B -> right out, soft-clipped, with a per-deck energy meter, pitch dot, and an Alt+PITCH model-selector readout shown while Alt is held (the 5 models around the ring, selected one bright) on the rings. Uses PIMPL (an opaque `Impl*`) to keep all Rings/stmlib types out of the header - mandatory because stmlib declares a global `namespace impl` that otherwise collides with `app.cpp`'s composition-root instance. Built at `-Os` (Rings overflows execution SRAM at `-O2`); fits ~96% (183 KB / 186 KB).

- **Vendored Rings/stmlib DSP, trimmed and colocated under the engine.** reso's Rings + stmlib sources live under `src/engine/reso/thirdparty/` (colocated with the only engine that uses them; the `RESO_TP`/`RESO_INC` make vars scope the source list and the `-I` to the reso build, so other engines never see them). The tree is pruned to the exact compile closure - computed from the compiler's own `.d` dependency output - at ~34 files / ~320 KB, down from the ~50 MB upstream checkouts (the bulk was `stmlib/third_party` and Rings' `hardware_design`/`drivers`/`ui`/`bootloader`). stmlib's `LICENSE` is kept; Rings' MIT notice lives in each retained source-file header.

- **Headless reso test.** `make -C host test-reso` builds and runs `host/test_reso.cpp`, which drives `ResoEngine` through the public `IEngine` surface (stub `ITransport`): param round-trip + mode-switch reporting, Slice decay, Reel quiet-idle / sounds-on-trigger, Drift scatter, MIDI note, autocorrelation pitch-tracking, brightness control, the **V/Oct-CV-must-not-clobber-the-knob** regression, and the **audible-default-ENV** guard (default ENV plucks; ENV fully CCW is near-silent).

- **Opt-in CMake build (parity-targeted alternative to the Make build).** A second build system lands alongside the canonical `Makefile`, driven by `make -f Makefile.cmake ENGINE=<engine>` - a thin frontend preserving the `make` command surface (`program-dfu`, `engine-<name>`, `DEBUG=1`, `LOFI_INT16=1`). `CMakeLists.txt` reproduces the Make build's hardware-critical specifics rather than CMake defaults: `BOOT_APP` (skips the SRAM bootloader's re-`SystemInit`), the split opt levels (libs `-O3`, app `-O2`, reso `-Os`), the `alt_sram.lds` linker script, strict `-std=c++17 -fasm -fshort-enums`, and the bleeptools `midi_util.cpp` fork-gap. Each engine caches in its own `build-cmake/<engine>/` dir, so switching engines never forces a rebuild (retiring the `.engine-stamp` clean/rebuild dance). The Make build stays canonical and untouched; adopting CMake (renaming the frontend to `Makefile`) is a deferred option. Verified: the CMake-built reso firmware flashes over DFU and runs identically to the Make build on hardware (the residual ~4 KB `SRAM_EXEC` delta is section layout, not behaviour).

- **Edrums becomes a four-drum engine (two drums per deck).** Each deck now holds two drums in a slot pair; all four sequence and sound at once, but a deck's seven knobs and its ring address only the **focused** drum. Tapping a deck's **Rev pad** swaps the focus to the other drum (the backgrounded one keeps playing with its stored parameters). On a swap the engine flags a one-shot re-seed and the platform re-seeds that deck's knob pickup from the newly-focused drum's values, so the pots take over without jumps. Display is per-`(deck, slot)`: the ring shows the focused drum in its colour (A amber/magenta, B cyan/violet) and the **Rev LED** carries the backgrounded drum's colour. Default kit: deck A = Kick + Tom, deck B = Snare + Hat; all four voices are summed with a pre-limiter trim so the hotter four-voice bus avoids constant limiting. The deck→slot mapping lives entirely inside the engine, so the platform stays two-deck.

- **`IEngine::take_param_reseed` — a generic knob re-seed request.** A defaulted (`false`) one-shot the platform polls each loop; when an engine repoints a deck's knobs at a different underlying target (edrums' drum swap), it returns `true` once and the platform re-seeds that deck's `MValue` pickup cache from `param()`. Engines with a fixed deck→value mapping need nothing, so granular/passthrough/delay are unchanged.

- **`IEngine::set_aux_active` — a per-loop Alt-held UI hint.** A defaulted (no-op) hook the platform pushes each loop for `CapAux` engines: `true` while Alt is held and that deck's PITCH is not claimed by an fx touch. It lets an own-display engine show its Alt+PITCH selector the *whole time* Alt is held rather than only flashing on a change - reso draws its 5-model ring readout from it. Engines that don't draw an Aux selector ignore it.

- **Headless edrums slot test.** `make -C host test-edrums` builds and runs `host/test_edrums_slots.cpp`, which drives `EdrumsEngine` through the public `IEngine` surface (with a stub `ITransport`) to pin the focus swap, slot independence, the re-seed one-shot, and finite four-voice output.

### Changed

- **ENV / SIZE / MODFREQ knob seeds became engine-authoritative.** Extending the `Aux`/`ModAmp` pattern from 0.2.1, the platform now seeds the ENV, SIZE, and MODFREQ (`ModSpeed`) knob start positions from each engine's `param()` cache instead of shared `core.ui` literals, so every engine chooses its own defaults. reso seeds **ENV 0.5** (its ENV drives Rings brightness, and at 0 the excitation is filtered to silence, so a 0 default booted the voice silent-on-trigger), **SIZE 0.5** (damping decay is steep near the top, so the knob was over-sensitive at the old 1.0), and **MODFREQ 0** (Slice arp / Drift scatter off, so it no longer free-runs from boot). granular and delay carry their prior 1.0 SIZE / 0.3 MODFREQ in their own caches and 0.0 ENV, and edrums its own per-slot seeds, so non-reso engines are unchanged.

### Fixed

- **Removed dead `C_USR_FLAGS` build config (verified no firmware change).** The Makefile set `C_USR_FLAGS = -ffast-math -funroll-loops`, but libDaisy's core Makefile reads `C_USER_FLAGS` (with the E), so the flags were silently dropped - they never reached the compiler and the shipping firmware was always built without fast-math or loop unrolling. Replaced the line with a comment recording this rather than "correcting" the variable name: activating the flags would change behaviour on a hardware-critical path (`-ffast-math` implies `-ffinite-math-only`, dropping `isnan`/`isinf` guards; `-funroll-loops` inflates `.text`, and `SRAM_EXEC` is already ~94% full), so enabling them is a deliberate, hardware-flashed change to batch with P2, not a typo fix. Verified a no-op on the host: the `make -Bn` compile line is byte-identical before and after.

- **Re-repaired the `test/` unit harness after the DSP restructure.** The shared-primitive move (`divider` -> `src/dsp/`, `synclock` -> `src/transport/`) left `test/Makefile` pointing at the now-defunct `src/engine/granular/` paths, so `make -C test` could not build. Repathed the unit sources and added `-I../src/transport`; the suite builds and passes again (116 assertions).

- **Restored the `host/` granular harness after the transport decoupling.** `main_host.cpp` and `test_engine_params.cpp` still called the `transport_*` forwarders removed from `IEngine`/`GranularEngine` when the clock became a platform service, so neither compiled. Both now construct the real platform `Transport`, inject it via `EngineContext` before `init()` (so the granular `Core` subscribes to its ticks), and drive `transport.tick()` per block where they used `engine.transport_tick()` - the same injection the firmware does. Wired `src/transport/*.cpp` (and its include dir) into `host/Makefile`, and `make -C host test` now runs both the granular (`test-granular`) and edrums (`test-edrums`) engine tests.

## [0.2.1]

### Added

- **Euclidean dual-drum engine (`edrums`).** `make engine-edrums` builds a two-track (deck A/B) drum machine - the first engine to *sequence* off the shared transport (it subscribes to the clock's ticks rather than only reading tempo). Each deck is a Euclidean pattern (`dsp/CPattern`, made variable-length here) driving a synthesized voice (a sine body with a fast pitch sweep plus a band-passed noise component, shaped by an exponential amp envelope). Per-deck controls: POS = density (onsets), SIZE = pattern length, ENV = rotation, PITCH = pitch + noise colour, SOS = decay, MOD_AMT = onset probability, MODFREQ = clock division (1/16, 1/8, 1/4) - so the two tracks can run at different lengths and rates (polymeter), realigned to the bar on an external-clock reset. Five drum models per deck (kick / snare / clap / closed-hat / tom), selected live with Alt+PITCH; the clap is a multi-burst voice, and a model change briefly shows the model number on the deck's ring. The hardware routing switch picks the output: mono drum bus (centre), A/B split (left), or per-hit random pan (right). Advertises `CapOwnDisplay | CapDualDeck | CapAux`; the rings show each deck's pattern + playhead.

- **`ParamId::Aux` + `CapAux`: a reusable Alt+PITCH selector layer.** A platform channel for engines that want Alt+PITCH as a per-deck selector (edrums uses it for live drum-model select). The platform routes Alt+PITCH to `ParamId::Aux` only for engines that advertise `CapAux`; without it, Alt+PITCH keeps its existing meaning (granular's pitch-quantize toggle), so the granular build is unchanged. The `Aux` and `ModAmp` knob seeds also became engine-authoritative (read from `param()` like `Pos`), letting an engine choose their defaults (edrums: probability 100%, models A=kick / B=snare). (`src/engine/engine_params.h`, `src/ui/core.ui.{h,cpp}`)

### Changed

- **Transport/clock decoupled into a shared platform service.** The clock/transport (the old granular `Driver`) was split into an engine-agnostic platform `Transport` (`src/transport/`, compiled into every variant) and a granular tick-sink (`Core::_on_transport_tick`). The platform now owns the clock and injects a read-only `ITransport` (`src/engine/itransport.h`, carrying a `TransportTick {index, tick, key, quarter, tempo, reset}`) into engines via `EngineContext`; the `transport_*` forwarding group and `transport_leds()` were removed from `IEngine`. `SynClock`/`Tempo` moved into `src/transport/`, the shared clock-`Divider` primitive into `src/dsp/`, and the granular `driver.{h,cpp}` was deleted. Clock-source / tempo / key-interval control and the clock indicator LEDs are now platform-owned and render for every engine. Granular timing is behaviour-preserving; `src/transport` is added to the `check-boundary` guard.

- **The delay engine is now tempo-synced.** With the shared transport in place, the delay's SIZE knob selects a musical division (1/16T..1/2, incl. dotted + triplet) and the delay time locks to the transport tempo (smoothed so tempo changes glide); the tap buffer was grown to hold the longest division at the slowest tempo.

- **`.cpp`-bearing primitives moved into the `src/dsp/` shared tier.** `git mv` of `biquad`, `follower`, `adenv`, and `cpattern` (`.h` + `.cpp`) from `src/engine/granular/` into `src/dsp/`, joining the header-only batch seeded in Phase 5 R4. Their `.cpp` now compile from a new global `$(wildcard src/dsp/*.cpp)` in `CPP_SOURCES` so every engine links them, and the granular consumers reach them through the `dsp/` include prefix (`vox.h`, `fx.h`, `echo.h`, `modulator.h`), matching the platform/engine boundary convention. `biquad.cpp`'s `#include "../../common.h"` becomes `"common.h"` (same `src/common.h`, via `-Isrc`). Granular codegen is unaffected (identical TU set; `SRAM_EXEC` 185920 B, +8 B object-reorder alignment); the non-granular engines now compile these TUs but `--gc-sections` strips every unused symbol, so passthrough/delay are size-unaffected and link cleanly. Host build (`host/Makefile`) wired with the same `src/dsp/*.cpp` glob.

### Fixed

- **Restored the host unit-test harness (`test/`).** `test/Makefile` had been broken since the Phase-5 R3 `src/core` -> `src/engine/granular` move: it still pointed at `../src/core/*` sources and `-I../src/core`, none of which exist, so `make -C test` could not build. Repathed the unit sources to their current homes (`divider`/`synclock` under `src/engine/granular/`, `follower` under `src/dsp/`) and corrected the include roots. The suite builds and passes again (116 assertions).

- **App-composition tier moved into `src/` (root hygiene).** `git mv` of `app.{h,cpp}` and `meter.h` from the repository root into `src/`, leaving the root as just the entry point (`main.cpp`). Same motivation as the earlier `common.h` root->`src/` move: a coherent "the library lives in `src/`" rule, and it clears the latent `-Isrc` fallback smell (`meter.h`'s `#include "nocopy.h"` now resolves as a `src/` sibling rather than via the include-path fallback). `main.cpp` stays at root as the thin entry; its `#include "app.h"` resolves through `-Isrc`. The `build/app.o` engine-stamp rule is unaffected (libDaisy flattens objects to `build/` by basename), and `check-boundary` is unaffected (`src/app.cpp`, the composition root, is exempt and not under the scanned `hw/ui/memory` dirs). Purely mechanical (3 git renames; `SRAM_EXEC` byte-identical at 185912 B / 97.61% before and after).

## [0.2.0]

### Added

- **Stereo delay engine (second full engine).** `make engine-delay` builds a dual-tap stereo delay (deck A = left, deck B = right) with fractional-interpolated delay lines: SIZE sets delay time, POS the feedback, the mix knob the wet/dry, and PITCH transposes the delay taps +/-1 octave via a crossfading two-head pitch shifter. Advertises `CapOwnDisplay` (blue pitch rings). Proves the platform hosts a non-granular engine end to end.

- **Engine-drawn panels via `render(DisplayModel)` + the `CapOwnDisplay` capability.** An engine that advertises `CapOwnDisplay` fills the hardware-agnostic `DisplayModel` (rings + named indicators) in `render()`; the platform calls it from the main loop and blits it in the LED ISR (`_blit_display`), bypassing the granular `*_leds`/`render_ring` query path. The granular engine (no `CapOwnDisplay`) keeps its query+interpret path unchanged. The bundled `PassthroughEngine` now draws a live input level meter + lit play indicators instead of blank rings.

- **One-shot variant flash targets.** `make engine-granular`, `make engine-delay`, and `make engine-passthrough` do clean -> build -> flash (`program-dfu`) in a single command (device in DFU mode first). (`Makefile`)

- **`compile_commands.json` for clangd.** Generate it from a real build with `bear -- make` so the editor sees the actual include paths (the libDaisy header set, `-Isrc`), the `-std=c++17`, and the build-time `-DSPK_ENGINE_*` define. It is git-ignored and is a snapshot of whichever engine was built (granular by default); regenerate after structural changes. Tooling only - does not affect the firmware. (`.gitignore`, README)

### Changed

- **Platform/engine boundary cut and enforced (Phase 5, round 4).** The platform (`hw/`, `ui/`, `memory/`) now reaches the engine only through the contract headers in `src/engine/` - it includes zero granular DSP headers. The remaining couplings were dead/stale/misplaced code, not real DSP dependencies: the tempo BPM<->normalized range moved off granular `Tempo` into `config.h` (`kTempoMin/MaxBpm` + `tempo_abs_to_norm`/`tempo_norm_to_abs`); the `kKeyInterval` enum was lifted to the contract (`engine/mode.h`); the self-contained PCM utilities (`pcm_loader`/`pcm_convert`/ `sample16`) moved to `src/memory/`; `storage.h` now includes `engine/deck_ref.h`; and stale `core.h`/`granular_engine.h` includes were dropped. A `make check-boundary` target (wired as a prerequisite of `all`) fails the build if any `hw/`/`ui/`/`memory/` translation unit reintroduces an `engine/granular/` include, so the boundary is enforced by the build rather than by review. `app.cpp` is exempt as the composition root.

- **New `src/dsp/` shared-primitive tier.** Engine-agnostic DSP primitives that more than one consumer needs now live in `src/dsp/`, with the dependency flowing platform/engine -> dsp and never the reverse. Seeded with `lutsinosc.h` (sine LUT, used by the UI's LED rendering and the granular LFO) plus the header-only `smooth.h`, `deline.h`, and `hann.h`. The `common.h` shared header, previously sitting at the repository root by mistake, also moved into `src/`.

- **Granular DSP relocated to `src/engine/granular/` (Phase 5, round 3).** `git mv src/core` -> `src/engine/granular/`, with the two contract headers (`engine_context.h`, `itimesource.h`) moved up to the contract root `src/engine/`. All `#include "core/..."` references repathed across the tree and both Makefiles. The source tree now reads as one platform plus per-engine subdirectories (`granular/`, `delay/`, `passthrough/`). Purely mechanical (64 git renames; SRAM/SDRAM byte-identical before and after).

- **`EngineBuffers` generalized to an opaque SDRAM arena.** `EngineContext` now carries a single `EngineArena {base, bytes}` instead of granular-shaped buffer pointers; each engine sub-allocates its own buffers with a bump allocator (`src/engine/arena.h`). The SDRAM pool (`hw/buffer.sdram`) hands out one 48 MB block and no longer references any granular buffer (`source/detect/delay/slices/track`)

  - removing the last hardware-layer dependency on the granular engine. Granular's `Core::init` sub-allocates from the arena preserving the exact sizes, 32 KB alignment, and zeroing (behaviour- identical). Reclaimed ~4.2 KB of `SRAM_EXEC` as a side effect.

- **Compiler-enforceable platform/engine boundary (Phase 5, rounds 1-2).** The `IEngine` contract no longer pulls any granular `core/` types: the A/B selector (`DeckRef`), `Mode`/`Route`, the LED-query enums (`ModType`/`GritMode`/`DeckSource`), and the transport clock source (`ClockSource`) were relocated into contract headers (`src/engine/deck_ref.h`, `src/engine/mode.h`); the granular classes alias them, so their internals are unchanged (a duplicate `ModType` in `core.h` was unified away). `EngineBuffers` was type-stripped (`void*` + counts, casts in `Core::init`) so `engine_context.h` carries no granular types; `LEDRing` + `Color` moved `src/ui/` -> `src/engine/` so `DisplayModel` has no `ui/` dependency. All behaviour-preserving (codegen-neutral) and verified across the granular, passthrough, and host builds. The granular DSP `Driver` stays inside the granular engine; relocating it to a platform transport service is deferred to a transport-capable engine.

- **Engine file layout + build selection.** Engines live under `src/engine/<name>/` (`passthrough/passthrough_engine.h`); the `ENGINE` Makefile variable now selects the engine sources so a non-granular build doesn't compile the granular DSP at all. A `build/.engine-stamp` dependency rebuilds the engine-dependent object when `ENGINE` changes, so switching variants needs no `make clean`. See `docs/engine-layout.md`.

### Fixed

- **Host harness restored.** `make -C host` and `make -C host test` had stopped compiling when the `engine.core()` escape hatch was removed (both reached the graph through it). Rewritten onto the public `IEngine` API (`transport_*`, `set_config`, `on_*_pad`); the one white-box assertion that read internal fx state was dropped (no longer observable by design).

## [0.1.3]

### Fixed

- **Granular slice fade-out at high pitch/speed (V6).** `Vox::_set_decay_start()` assigned the grain decay-start position only when the fade region was shorter than the slice; in the high-increment, small-slice corner (e.g. playhead increment 8.0 with the minimum slice of 1344 samples, where the fade region 1536 exceeds the slice) it left a stale, history-dependent value. That produced an inconsistent fade trigger - grains cut short, or ending without their ~4 ms Hann fade and clicking at the boundary. The function now always assigns `_decay_start = std::max(0.f, _size - decay_length)`, so when the fade is longer than the slice the whole grain becomes the fade. Deterministic and bounded. Consistent with the manual's "slice-mode overdub may glitch when synced to MIDI clock." This is the only change in this release that alters runtime audio behaviour; validate by ear at maximum pitch on the smallest slice.

- **Stack buffer overflows in SD-card path building.** Five `char` buffers used to build tape/file paths were each one byte too small for their string plus the null terminator (`tape_dir_path[4]` for `"SK/G"`, three `audio_path[11]` for `"/SK/G/1.WAV"`, two `name[5]` for `"1.WAV"`). The `sprintf` calls wrote the terminating null one byte past the end - benign-by-luck via stack padding, but undefined behaviour. Buffers resized to fit; surfaced while replacing `sprintf` (see Changed).

### Changed

- **Platform/engine decoupling: the UI/HW platform no longer touches the DSP core.** A multi-round, behaviour-preserving, hardware-verified refactor inserted an `IEngine` layer (`src/engine/iengine.h`) between the fixed platform (`CoreUI`/`Storage`/`Hardware`) and a swappable DSP engine (the granular looper, now `GranularEngine`). The platform drives the engine entirely through `IEngine`: parameters (`set_param`/`param` keyed by `ParamId`), categorical config (`set_config` for route / deck mode / LFO shape / mod flags, plus `toggle_grit_mode` and `tempo_to_fit`), knob layout (`deck_layout`/`size_sets_tempo`, so the platform branches the SIZE/ENV knobs without reading the engine's `Mode`), MIDI, pads, CV/gate, the tape-storage audio port, transport, and the DAC mod-CV output. The `engine.core()` escape hatch and the concrete `GranularEngine&` constructor are gone - `CoreUI` holds only `IEngine&`, `Storage` an `IEngine*` - and `src/core/` is now platform-independent (no libDaisy include; sample rate, block size, buffers and the clock are injected via `EngineContext`/`ITimeSource`). The per-control MValue pickup state was rekeyed by `ParamId` (one array replacing ~21 named members). `capabilities()` gained its first real consumer: `Storage` skips tape save/load/preload unless the engine advertises `CapTapeStorage`. No runtime audio behaviour change in the granular build (verified by flashing); the layer is additive, so `SRAM_EXEC` rose to ~99.95% used (~88 B free) - the cost is amortised across future engines that reuse the whole platform.

- **SD path building uses manual string joins instead of `sprintf`.** All five `sprintf` calls (plain `%s` concatenations) were replaced with `strcpy`/`strcat`. This lets the linker drop the `sprintf`/`fprintf` objects, recovering ~648 B of the nearly-full `SRAM_EXEC` region (99.05% -> 98.71% used). Note: the core `vfprintf`/`dtoa` machinery (~2.3 KB) remains, pulled in by libDaisy's `logger.o` via `vsnprintf` - reclaiming it would require a libDaisy-side change.

These are correctness/intent fixes that do not change current runtime behaviour; they harden the code against future changes.

- **`Config::_is_loaded` default-initialised (V1).** Added `= false` to the flag backing `Config::is_loaded()`. In current usage `Config` exists only as the zero-initialised static singleton (`Config::dynamic()`), so the flag was already reliably `false`; this guards against any future non-static instantiation reading an indeterminate value.

- **Tape navigation uses `kStorageTapeCount` (V2).** `DeckStorage::next_tape`/ `previous_tape` and the `kTapeName[]` array were sized/wrapped on `kStorageSlotCount`. Both constants are 6 today, so the generated code is unchanged; the fix uses the semantically correct constant so tape handling stays correct if tape count ever diverges from slot count.

### Added

- **Swappable DSP engine, selected at build time.** The firmware is now a fixed hardware/UI platform that hosts one `IEngine` implementation per build. `make` builds the granular looper (default); `make ENGINE=passthrough` builds a minimal stereo-passthrough variant (`capabilities() == 0`, ~150 KB vs the granular ~186 KB as the granular DSP is dropped). Selection is one build-time choice: the `ENGINE` Makefile variable emits a single `-DSPK_ENGINE_*`, which `src/engine/engine_select.h` maps to the concrete `ActiveEngine` that `app.cpp` instantiates as a static member; the platform only ever sees `IEngine` (no per-feature `#ifdef`s). A stamp dependency (`build/.engine-stamp`) rebuilds the engine-dependent object when `ENGINE` changes, so switching variants needs no `make clean`. Both variants verified on hardware via `make program-dfu`. (`src/engine/engine_select.h`, `src/engine/passthrough_engine.h`, `Makefile`, `app.cpp`)

- **Off-target engine harness** (`make -C host`): runs the real `Core`/`GranularEngine` over WAV through the `IEngine` interface, with `make -C host test` asserting the parameter API across all deck modes. Does not build or affect the firmware; the pot/pad/LED/MIDI hardware paths remain hardware-verified only. (`host/`)

- **In-repo developer documentation** under `docs/`: `architecture.md`, `source-guide.md`, a firmware-tracked `manual.md`, and `review-260529.md` (a code/architecture review with verified and re-validated findings).

- **Host-only test harness** under `test/` (`make -C test test`). Compiles the pure-logic timing classes natively (no ARM toolchain, no hardware) and asserts their invariants. It does not build or affect the firmware. Initial coverage locks the two findings that required manual tracing: `Divider` triplet timing does not drift over long runs (S1), and `SynClock` emits exactly `_ticks_per_clock` internal ticks per external clock pulse for 24->48 and 12->48 PPQN, including when the internal timer is over-driven (S3). Also covers the `Config` text parser (S5): valid parse, the `is_loaded()` default (V1), and the documented brittleness (mid-token whitespace stripping, unknown/overlong names ignored); the `wav.h` header builder/parser (canonical 44-byte layout, build->serialize->parse round-trip, rejection of short/non-RIFF input); and `SpeedMap` (semitone->speed table: octave/fifth anchors, end clamping, monotonicity). A `test/stubs/daisysp.h` shim supplies the few DaisySP symbols referenced by otherwise-pure headers (`fmap`, `fclamp`); and `Follower` (mean-square envelope follower: reset, steady-state convergence, output clamping, amp scaling, attack-faster-than-release); the `sample16` float<->int16 conversion (round-trip, clamping, quantization step); `convert_pcm_block` (the load shim: f32<->i16 block conversion, clamping, and float->i16->float round-trip within a quantization step); and `PcmLoader` - a simulation of the SD audio-load accounting (frame counts, truncation to capacity, termination, contiguous placement) across every width combo and many chunk sizes, exercising the exact code card.cpp runs. 116 checks across 9 classes.

- **`LOFI_INT16` 16-bit loop buffer (default off, opt-in via `make LOFI_INT16=1`).** Stores the loop buffer as 16-bit PCM instead of 32-bit float, **doubling record time to 84 s in the same SDRAM** (verified: SDRAM footprint identical at both depths; `SRAM_EXEC` +304 B for the conversion code). Pieces:

  - `src/core/sample16.h` - pure, host-tested float<->int16 convert with hard clip.

  - `Buffer::Frame` becomes `int16_t` under the flag (`Buffer::Sample` alias); conversion is confined to the two element-wise sites, `Buffer::_read` and `Buffer::write` (the overdub mix stays in float, then clamps + quantizes). Identity / byte-identical codegen when the flag is off.

  - `kSourceMaxSeconds` 42 -> 84 under the flag; `wav.h` emits a 16-bit PCM header; the SD load path (`card.cpp`) accepts the matching depth and computes frames from the correct byte width.

  - `Makefile` gains a `LOFI_INT16=1` build option (mirrors `DEBUG=1`). Audio storage behaviour in the default build is byte-identical (the `int16` paths compile out); the default save format is still 32-bit float.

- **Convert-on-load shim (`src/core/pcm_convert.h`, wired into `card.cpp`).** The SD load path now accepts BOTH 16-bit PCM and 32-bit float files in either build and converts to the buffer's storage width on the fly (`convert_pcm_block`, pure + host-tested). So a 16-bit firmware loads legacy float tapes and a float firmware loads 16-bit tapes - resolving the prior "rejects mismatched tapes" limitation; tapes are portable across builds. `kChunk` (32 KB) is a multiple of both sample widths and WAV data is sample-aligned, so chunks never straddle a sample (no carry buffer needed). Loading a longer tape than the buffer holds truncates to capacity. The streaming size/offset/ termination math lives in a pure, host-tested helper (`src/core/pcm_loader.h`, `PcmLoader`) that card.cpp delegates to, so the accounting is validated off-target rather than by reasoning alone. Note: this adds the dual-format acceptance + converter to the **default** build too (~+150 B `SRAM_EXEC`), so the default firmware now also reads 16-bit tapes - a small, additive behaviour change. The audible overdub-quantization character of the 16-bit buffer remains unverified by ear. See `docs/dev/lofi-int16-scope.md`.

- **Design scope docs** `docs/dev/lofi-path-b-scope.md` (half-rate buffer) and `docs/dev/lofi-int16-scope.md` (16-bit storage): inventories of the frame<->time/tempo coupling and the change sites for each lo-fi record-time approach.

### Fixed (portability)

- **`divider.h` now includes `<cmath>` for `std::round`** (was relying on `<math.h>`, which only places `round` in `std::` on the ARM toolchain, not in standard libc++). Surfaced by the new host harness; no effect on the firmware build. (`src/core/divider.h`)

- **`WavHeader::size` retyped `size_t` -> `uint32_t`.** The RIFF chunk-size field is a 32-bit field per the WAV spec; `size_t` is 4 bytes only on the 32-bit target, so the 44-byte layout (and its `static_assert`) held there but broke on a 64-bit host and would emit a malformed header on any 64-bit build. Layout-identical on-target (verified: the firmware binary is byte-for-byte unchanged in size); `wav.h` also now includes the `<cstdint>`/`<cstddef>`/`<cstring>` it uses rather than relying on its includers. Surfaced by the host harness. (`src/memory/wav.h`)

- **`follower.cpp` now includes `<cmath>` for `std::exp`** (was relying on `<math.h>`; same std-namespace portability issue as `divider.h`). Surfaced by the host harness; no effect on the firmware build. (`src/core/follower.cpp`)

## [1.0.2]

Baseline release prior to this changelog. See Git history for details.
